분류: **SSOT**. 스펙과 구현 사이에 남은 갭을 추적하고, 다음 행동을 본다.

# GAP-MATRIX — 갭 추적

> 갭이 채워지면 상태를 `DONE`으로, 진행 중이면 `IN_PROGRESS`로, 팀 회의 대기면 `BLOCKED`로 바꾼다.

| 갭 | 영향받는 기능 | 상태 | 다음 행동 |
|---|---|---|---|
| **자동 게이트가 실렌더·통합 검증을 안 집는다** — `all.sh` 가 `*.sh` 만 돈다 | **완료 판정 전부 · 회귀 탐지** | **OPEN — P0 1순위 (2026-08-05 `/감사`)** | `.mjs` 7개(`fr5-web`·`fr5-bridge`·`dash-web`·`cam-web`·`tb-*`)를 **어느 `.sh` 도 안 부른다**(grep 0건). 오늘 실행 로그에 `실렌더` 0건 — 돈 것은 `.sh` 16개뿐. 핸드오프의 "단위 106·브리지 82·실렌더 46" 중 **자동은 106 하나다.** 이걸 먼저 고쳐야 나머지 수리의 회귀를 잡는다 (`evidence/2026-08-05/audit-harness-and-bridge.md`). **2026-08-06 추가 — 그냥 못 집는다.** ①`.mjs` 9개 중 **6개가 `/Users/family/jason/FR5Web/.claude/...` 절대경로로 import** 한다(남의 기계에서 죽는다) ②`fr5-web-verify` 의 `OUT` 기본값이 **끝난 세션의 스크래치패드**를 가리키는데 `cdp-harness` 의 `screenshot` 은 `writeFileSync` 뿐이라 폴더를 안 만든다 ③`tb-real-verify` 는 실물 TB 가 필요해 게이트에 넣으면 안 된다 ④감사가 기록한 "관제 17/28" 이 맞다면 **집는 순간 `all.sh` 가 빨개진다** — 그 결정은 사람이 한다. **2026-08-06 밤 — FR5 몫 156건은 잠갔다** (`check/fr5-render.sh` 신설 · `all.sh` 3회 연속 exit 0 · 결함 주입 2종으로 빨개지는 것 확인). 남은 것은 `dash-web`(113)·`cam-web`·`fr5-cam`·`tb-*` 다 |
| **게이트에 넣는 순간 드러난 것 — `.mjs` 가 자기 자식을 안 죽인다** | **게이트 편입 전부** | **CLOSED 2026-08-06** | `uv run` 도 `npm run` 도 SIGTERM 을 자식에게 안 넘긴다. `.kill()` 만 하면 **uvicorn 이 5155·5157 을, vite 가 5176 을 쥔 채 살아남아** 다음 실행이 첫 검사부터 무더기로 실패한다. **손으로 한 번씩 부를 때는 절대 안 보인다** — 그래서 "손으로 부르는 검증" 은 연달아 돌려 본 적이 없었고, 이게 `.mjs` 를 게이트에 못 넣던 진짜 이유였다. `detached: true` + 프로세스 그룹 `SIGKILL` + `process.on('exit')` 로 고아 0 |
| **폰 보기 전용이 계획서에만 있다** — 폰에서 조그·캡처·승인이 그대로 눌린다 | **실기 오조작 (주머니 속 터치)** | OPEN — P1 (2026-08-06) | 계획 §폰(`FR5-IMPLEMENTATION-PLAN.md:90`)은 *"조작 컨트롤 자체를 내보내지 않아 **구조적으로** 없앤다"* 인데, 구현은 `FR5/src/screens/main.css:270` 의 `@media (max-width:760px)` **재배치 6줄뿐**이다 — 좁혔을 뿐 못 내보냈다. 계획서 자신도 `:88` 에서 미구현이라 적고 있다. STOP 만 남기고 `ControlDock`·캡처·승인을 렌더에서 빼는 것이 처방이고, 게이트는 **좁은 뷰포트로 열어 조작 버튼이 0개인지** 세면 된다 |
| **`.mjs` 검증이 고정 `sleep` 으로 비동기를 기다린다** — `fr5-bridge-verify` 에 21곳 남음 | 게이트 신뢰도 (거짓 실패) | OPEN — P2 (2026-08-06) | 300ms 가 모자라면 그 뒤 검사가 줄줄이 빨개져 **원인이 어디인지 안 보인다.** 흔들린 6곳은 `until()`(조건 폴링)으로 바꿨고 나머지는 아직 고정값이다. 같은 뿌리로 `fr5-web-verify` 의 `clickText` 가 `'notfound'` 를 조용히 삼켰다 → 버튼을 기다리고 못 찾으면 던지게 고쳤다. **2026-08-06 실측 — `all.sh` 6회 중 2회 빨갰고 둘 다 재실행에서 초록이었다.** 자리가 매번 달랐다(①`fr5-bridge-verify` 조종권 ②`fr5-web-verify` 의 `미연결 phase 표시 DISCONNECTED`— `p.waitFor` 가 아니라 `p.eval` 이라 재시도가 없다). **게이트가 흔들리면 진짜 회귀와 구분이 안 된다** — 남은 고정 `sleep` 과 재시도 없는 `p.eval` 단언을 `waitFor` 로 바꾸는 것이 처방이다 |
| ~~궤적 이름이 그대로 파일 경로가 된다~~ | 데이터 무결성 · 호스트 파일 | **CLOSED 2026-08-06** | `teach.py` 에 `safe_name`(`^[A-Za-z0-9가-힣_-]{1,64}$`) 신설. 녹화 **시작에서** 거르고(끝에서 막으면 120초 녹화 후 버린다), `TrajectoryStore._path` 가 `resolve()` 로 폴더 하위인지 한 번 더 본다 — `save`·`get` 둘 다 그걸 지난다. 단위 6건 + 실렌더 1건(`경로가 되는 궤적 이름은 거부된다`) |
| ~~`NaN` 이 안전 설정 대조를 조용히 통과한다~~ | **충돌 감지·드래그 티칭의 전제** | **CLOSED 2026-08-06** | `session._within` 신설 — `math.isfinite` 를 결측과 같이 취급한다. `cogMm` 은 **길이도 본다**(`zip` 이 짧은 쪽에서 조용히 멈춰 빈 목록이면 비교가 0회였다 · 감사가 안 짚은 같은 뿌리). `test_session.py` 신설 8건 |
| ~~지점 삭제가 확인도 되돌리기도 없다~~ | Teach 사용성 · 작업 손실 | **CLOSED 2026-08-06** | 카드 안 2단계 확인(`삭제` → `취소`/`지운다`). **`window.confirm` 은 안 쓴다** — 브라우저 모달이 화면을 막아 실렌더 검증·자동화가 그 자리에서 멈춘다. 표를 카드로 바꾸며 버튼 세로 29px→33px. 실렌더 2건 |
| **브리지 상태·예외 처리 결함 6건** — `read_state` 3필드 미독 · 진단 로그 2종이 항상 `None` · `forward_kin` 계약 위반 · 명령 경로 `fail_closed` 누락 · `GetSoftwareVersion` 만 `_guard` 밖 | 조건 6 · `EXECUTING` · 작업영역 게이트 | **OPEN — P1 (2026-08-05 `/감사`)** | **표적 ① 의 근원이 여기다** — SDK 의 `robot_state`·`motion_done`·`actual_qd` 를 안 읽어 전부 `mc_queue_len` 하나에 매달려 있다. 목록·근거는 `evidence/2026-08-05/audit-harness-and-bridge.md` §P1 |
| **조종권·오리진 구멍 2건** — HTTP 로만 claim 하면 자동 해제가 안 걸린다 · Origin 검사 0건 | 실기 조작 안전 · 팀 협업 | **OPEN — P1 (2026-08-05 `/감사`)** | `owner.py:71`↔`main.py:158` — `_disconnected_at` 이 WS 종료에서만 채워져 **서버 재시작 외 복구가 없다.** WS 는 동일출처 정책 밖이라 임의 웹페이지가 명령을 쏠 수 있다 — LAN 신뢰 전제의 경계를 계약에 명시할지 정한다 |
| **웹 화면 손 사용성 6건** — `busy` 영구 잠김 · `.catch` 없음 · 조그 연타 · 복제가 폰에 길 없음 · 툴팁 전용 안내 · STOP 26px | 폰 사용성 · 실기 오조작 | **부분 CLOSED 2026-08-06 — 남은 것 2건** | **닫힘 4건 (2026-08-06 `/감사` UI/UX)** — ①`busy` 영구 잠김: `LivePanel.run()` 만 `try/finally` 가 없어 브리지가 못 답하면 연결·조종권·ARM·DISARM 이 전부 죽었다(Teach·Program 은 이미 있었다) ②`.catch` 없음: 세 패널 `run()` 이 이제 못 닿음을 문장으로 말한다 ③툴팁 전용 안내: `title` 을 걷어내고 `capture-blocked`·`goto-blocked`·`rec-blocked`·`claim-blocked`·`arm-blocked`·`write-blocked` 문장으로 바꿨다 ④`observe-only`·`OBSERVE_ONLY`·`tool0/user0`·`demo·measure·done` 에 한글을 붙였다. **남은 것: 조그 연타 · STOP 26px** |
| ~~읽기 실패를 "없음" 이라고 말한다~~ | **화면이 거짓말 · 작업 재생산** | **CLOSED 2026-08-06 (`/감사` UI/UX · P0)** | `TeachPanel.reload()`·`ProgramPanel.reload()` 가 실패를 삼켜 목록이 `[]` 로 남고 화면이 *"아직 지점이 없습니다"*·*"아직 프로그램이 없습니다"* 라고 했다 — 서버에는 멀쩡히 있는데 학생이 처음부터 다시 만든다. SAFETY-RULES 제1원칙("못 왔으면 못 왔다고 말한다")의 화면판. `loadErr` 상태 + `points-loaderr`·`trajs-loaderr`·`slots-loaderr` 로 **없음과 못 읽음을 가른다** |
| ~~프로그램 삭제에 확인이 없다~~ | 작업 손실 | **CLOSED 2026-08-06 (`/감사` UI/UX · P0)** | 지점 삭제는 확인을 붙였는데(`GAP` 위 15행) 프로그램만 한 번에 사라졌다 — 계획 §확인 절차는 *"삭제가 모두 이 형태다"* 다. `slot-delete` → `slot-delete-ask`(취소/지웁니다) 2단계. `window.confirm` 은 여기서도 안 쓴다 |
| ~~폰에서 카메라 PiP 가 데스크톱 크기 그대로 뜬다~~ | 폰 사용성 (3D 가림) | **CLOSED 2026-08-06 (`/감사` UI/UX · P1)** | `CamView.jsx` 의 인라인 `style={{width,height}}` 가 `main.css` 폰 미디어쿼리(`.camview{width:132px}`)를 **항상** 이겼다 — 인라인은 스타일시트보다 세다. 저장된 340px 이 폰에서 3D 를 절반 넘게 가렸고, 주석은 그걸 막으려던 것이었다. `matchMedia('(max-width:760px)')` 로 좁으면 인라인을 안 준다. 접힘·폰에서 리사이즈 핸들도 뺐다(끌어도 안 먹으면서 값만 저장됐다) |
| **Teach 가 tool/user 좌표계를 못 정한다** — 읽기만 된다 | 계획 §화면 2 필수 항목 | **OPEN — P2 (2026-08-06 `/감사`)** | 계획(`FR5-IMPLEMENTATION-PLAN.md:51`)의 Teach 필수는 *"이름·tool/user 좌표계"* 인데 `TeachPanel.jsx` 는 캡처된 값을 보여주기만 한다(`공구0 · 기준0`). 좌표계는 지금 브리지 `config.yaml` 이 정하고 화면에 길이 없다 — **`toolCoordId` 를 브리지가 로봇에 안 넣는 미해결(아래 92행)과 같은 뿌리**라 그게 먼저다. 급하지 않다: 실기가 한 좌표계로만 돌고 있고, 지점 실행은 승인 당시 `toolId`/`userId` 를 대조해 막는다 |
| **첫 화면 죽은 공간** — 연결 전 관절·TCP 12행이 전부 `—` | 초심자 진입 | **CLOSED 2026-08-06 (`/감사` UI/UX · P1)** | 340px 사이드에서 세로 대부분을 뜻 없는 대시가 먹었고, 정작 *"지금 무엇을 하면 되나"* 는 어디에도 없었다. 두 표를 `state.connected` 일 때만 그리고 그 자리에 `startguide`(이름→연결→조종권→ARM→조작 가능) 5칸을 넣었다. 안전바 `기록 —` 도 `기록 안 함` 으로 — 칸은 계획이 정한 8개 그대로 |
| **운영 회복탄력성 2건** — 배포가 로봇 재연결 실패해도 `exit 0` · ~~tb-bridge 감시 없음~~ | 배포 신뢰 · 관측 | **부분 CLOSED 2026-08-06 — 남은 것 1건** | ~~tb-bridge 감시~~ → **systemd `--user tb-bridge` 신설**(`scripts/robot/tb-service.sh` · `Restart=always`). `ssh host "… &"` 로 데몬을 만들려다 세 번 넘어진 끝에 FR5 와 같은 자리로 왔다 (D80 · `evidence/2026-08-06/tb-connect-check.md` §3부). **남은 것** — `deploy/fr5-ubuntu.sh:27` 의 `*)` 분기가 echo 만 하고 `배포 OK` 로 이어진다(절반만 된 배포가 성공을 보고한다) |
| **FR5 배포가 터틀봇을 죽였다** — `rsync --delete` 가 호스트에만 사는 `.venv`·실험 데이터를 지운다 | 터틀봇 실기 (조용한 파괴) | **CLOSED 2026-08-06** | 두 브리지가 같은 트리(`~/FR5Web/`)에 살면서 생긴 일이다. `fr5-ubuntu.sh` 에 `.venv`·`TurtleBot/bridge/data` 제외를 박았다. FR5 자기 데이터가 트리 **밖**(`~/fr5-data`)에 사는 것과 같은 이유인데, 터틀봇은 트리 안이라 명시로 지켜야 한다 |
| **P2 9건** — 죽은 `missing` 필드 · 이름/개수 상한 없음 · 예외 문구 노출 · `tb-*.mjs` catch 누락 · pyyaml 강등 · README 29↔실측 106 · URDF 실패 무고지 · 배치안 삭제 무확인 | 진단·문서 정확도 | **OPEN — P2 (2026-08-05 `/감사`)** | 근거는 `evidence/2026-08-05/audit-harness-and-bridge.md` §P2. 급하지 않다 |
| **글로벌 카메라 해상도** — USB 웹캠 640×480 고정 (1.9 px/칸) | AR 실제 맵 | **CLOSED (2026-08-04)** | 갤럭시 + IP Webcam 이 2560×1440. 유선(`adb forward`)·무선 처리량 동일 (`evidence/2026-08-04/global-cam-phone.md`). **08-06 대시보드 재검증 때는 현재 앱 설정이 1920×1080**이었다 — PiP 표시에는 문제없지만 캘리브레이션 전에는 `cam-lock.sh`와 해상도 재확인이 필요하다 |
| **겹치기 화면이 배치안을 하드코딩** — 고정물 전용이었다 | AR 실제 맵 · 대시보드 연결 | **CLOSED (2026-08-04)** | `?scene=`·파일 선택·`localStorage` 로 대시보드 내보내기를 그대로 받는다. 태그 사각형 `?fit=` 균일 배율 + 배율 HUD. `cam-web-verify.mjs` 12/12 · 정합 0.80px |
| **겹치기를 실측 캘리브레이션으로 안 돌렸다** — 12/12 가 전부 합성 고정물 | AR 실제 맵 | OPEN | `intrinsics.py`→`extrinsics.py` 가 `Shared/data/config/global-cam.json` 을 채우면 화면이 자동으로 그걸 집는다. 라이브 `?feed=` 는 스트림 주인 계약이 먼저다 (하드 룰 1) |
| **내부 파라미터 미측정** — `intrinsics.py` 를 아직 안 돌렸다 | AR 실제 맵 (다음 걸음) | OPEN | ChArUco 15~20장 → RMS ≤0.5px. **자동초점이라 장마다 렌즈가 다를 위험** — 거리 말고 각도·위치를 바꿔 찍는다 |
| **화각 미측정** — 검출 간격 역산 44° 는 추정 | 카메라 자리·맵 크기 판단 | OPEN | `intrinsics.py` 가 낸다. 1080p↔1440p 화각이 같은지도 미확인이라 **해상도를 바꾸면 내부 파라미터를 그냥 스케일할 수 없다** |
| **태그 실기 검출 미측정** — ChArUco 픽셀에서 환산한 17 px/칸뿐 | 4단계 (`extrinsics.py`) | OPEN | 인쇄한 태그를 바닥에 놓고 `aim.py` 로 실측. 환산은 근거가 아니다 |
| **거치 높이가 맵보다 작다** — 3250×3250 맵인데 카메라가 960mm | 글로벌 카메라 전체 | OPEN | 실측 HFOV **73.05°**(2026-08-07 재측정) 로 손계산: 960mm 에서 담기는 폭 **1.42m**(면적 19%), 3250 을 담으려면 **2.19m**. (옛 62.57° 로는 1.17m·13%·2.70m 이었다 — 그 값이 틀렸다) 울트라와이드로 낮출 수 있으나 왜곡이 커지고 IP Webcam 이 그 렌즈를 고를 수 있는지 미확인. **거치대가 먼저다** |
| **XR 이 실물 크기로 안 놓인다** — `TARGET_MM=2500` 으로 줄여서 얹는다 | 실제 맵 위 겹치기 | **CLOSED (2026-08-05)** | 촬영/평소 모드 — **차이는 배율 숫자 하나**. 두 모서리 탭이 위치·회전·배율을 한 번에 낸다. 회전 4종 × 축척 3종 계산 검증 **오차 0.0000°/0.0000%** (`evidence/2026-08-04/global-cam-phone.md`). **실기 탭은 아직 안 했다** |
| **화면 기록이 XR 배경을 담나** — 앱 안에선 원리상 못 담는다 | 포트폴리오 영상 전체 | **CLOSED (2026-08-05)** | 갤럭시 빠른 설정 `화면 기록`으로 담긴다. 외부 카메라로 폰째 찍는 대안 불필요. 앱 내 녹화는 여전히 불가 — 패스스루를 **시스템이 바깥에서 합성**한다 |
| **XR 이 바닥에서 뜬다** — 처음 화면에 따라 공중에 얹힌다 | 겹치기 신뢰도 · 걸어 들어가기 | OPEN | 원인 셋을 갈라 고쳤다 — ①`XRAnchor` 미사용(드리프트) ②`shadowMap` 꺼짐 + 슬래브 숨김 = 접지 단서 0 ③바닥 y 를 한 프레임에서만 골랐다. **캘리브레이션 문제가 아니었다.** 코드는 들어갔고 **실기 미확인** (`evidence/2026-08-06/xr-ground-and-walk.md`) |
| **답사 정합의 최대 오차가 손으로 돌리는 15° 였다** — 최선을 다해도 ±7.5° 가 남아 12m 지점에서 **157cm** (축척 오차 24cm 의 6배) | 답사 1:1 신뢰도 | **CLOSED (2026-08-07)** | 히트 포즈의 `orientation`(= 그 면의 법선)을 **받아 놓고 버리고 있었다**. 벽을 탭하면 각도가 공짜로 들어온다 + 훑기(3m·120표본 = **0.240°**, 손계산 예측 0.24° 와 일치). `↺↻` 는 15°→**90°** 로 남겼다 — 법선은 yaw 를 4지선다까지만 좁힌다 |
| **`다시 놓기` 가 앵커를 못 지웠다** — 앵커가 붙기 전에 누르면 `xrAnchor` 가 `null` 이라 삭제가 무동작이고, 뒤늦게 옛 앵커가 되살아나 **새로 놓은 맵을 매 프레임 옛 자리로 끌어갔다** | 겹치기 재배치 | **CLOSED (2026-08-07)** | 배포본에 있던 버그다. 놓기 세대 카운터로 무효화 (`xr.js` F1). 사람 손으론 타이밍이 잘 안 맞아 안 보였는데 **자동 놓기가 상시 경로로 만들 참이었다** |
| **FR5 로봇이 누워서 뜬다** | 모든 3D·AR 화면 | **CLOSED (2026-08-07)** | URDF Z-up→Y-up 회전이 **다섯 벌**로 복사돼 있었고 이름도 다 달랐다(`holder`/`stage`/`zUpToYUp`). 한 벌이 빠지면 눕는다. `Shared/view3d/robot.js` 의 `mountRobotYUp` 하나로 흡수 — **5 → 1**. 규칙은 그 파일 주석에 이미 적혀 있었고 구현만 없었다 |
| **놓아도 되는 때를 사람이 알 수 없었다** — `WARMUP_MS` 1.5초 타이머가 전부였다 | 정합 품질 · 사용성 | **CLOSED (2026-08-07)** | 시간은 틀린 것을 쟀다 — ARCore 를 수렴시키는 건 **시차**다. 시차·바닥 흔들림·훑은 범위의 **최솟값**으로 링이 차오르고, 화면엔 진단 대신 **다음 동작 한 줄**만 뜬다. 초록 순간 진동. **유예 6초 뒤엔 무조건 통과** — 삼각대에서 영영 안 열리는 도달 불가 상태를 만들 뻔했다 (F4) |
| **탭하기도 전에 맵이 서 있었다** — `anchor` 가 `local-floor` 원점(= 세션 열 때 서 있던 자리)에 방치됐다 | 겹치기 첫인상 | **CLOSED (2026-08-07 · 실기)** | 겨냥한 곳과 무관한 데라 "이미 놓였나?" 로 읽히고 탭하면 점프했다. 놓기 전엔 링만 그린다 — **링이 곧 "여기에 놓인다"** |
| **답사가 구석에 세워 놓고 걸어 들어가라 했다** — root 원점이 맵 모서리라 대각선으로 들어가야 했다 | **답사의 존재 이유** | **CLOSED (2026-08-07 · 실기)** | 링이 맵의 어디인지를 모드가 정한다(`PIVOT`) — 평소 가운데 · **답사 들어가는 변의 가운데** · 촬영 모서리(그대로). 이동은 `apply()` 에서 해야 확대해도 기준점이 안 밀린다. **주인님 실기 확인** (D76) |
| **벽 법선이 남긴 4지선다를 사람 손에 미뤘다** | 정합 사용성 | **CLOSED (2026-08-07)** | 직사각형은 90° 넷이 다 유효한데 기하학은 못 고른다. **사람이 서서 보는 쪽**이 고른다(`snapQuadrant`) — 벽이 정밀함을, 사람이 방향을. 게이트 전수 33×33: 90° 배수로만 돌려 정밀함 훼손 0, 사람 쪽 45° 안 |
| **`D42` 가 서로 다른 두 결정에 붙어 있다** — "실기가 낸 값을 기본값으로" 와 "실기 어댑터는 공식 Python SDK" | 결정 참조 전부 | OPEN | `DECISION-LOG.md:872`·`:908`, 목차도 `:51`·`:52` 두 행. **참조 14건**이라 번호를 바꾸면 같이 고쳐야 한다 — 사람이 어느 쪽을 옮길지 정한다. `rotate-decisions.mjs` 의 자가검사가 찾아냈다 |
| **훑기 잔차가 "제자리 팬" 은 못 잡는다** | 훑기 정렬 신뢰도 | OPEN — 한계로 문서화 | 서서 폰만 돌려도 점들은 바닥 평면 위 직선이라 잔차가 작게 나온다(실측 5.8mm). 그래서 **이동 거리를 따로 재서** 0.15m 미만이면 "옆으로 걸으며 다시" 를 띄운다. 검사로도 박아 뒀다 — 없앤 게 아니라 **안다** |
| **배치안 안을 걸어 들어갈 수 없다** — 겹치기가 1:2.5 축소뿐이었다 | 배치 사전 확인 · 시연 | **CLOSED (2026-08-06)** | 모드 넷으로 갈랐다 — 평소(1:2.5)·**답사(1:1 · 한 번 탭 · 걸어 들어간다)**·촬영(1:1+두 모서리 보정)·화면(XR 없이 · PC 확인용). 겹치기 셋은 여전히 한 코드 경로다. 가우시안 스플랫은 기각 — 찍을 실물 공장이 없고 치수가 없다. **실기 미확인** |
| **1:1 로 들어가면 벽이 실제 방을 가린다** — 불투명 벽 넉 장 | 겹쳐 보이기 | **CLOSED (2026-08-06)** | 겹치기 모드는 벽을 `opacity 0.16` + 모서리 선으로 바꾼다. 헤드리스 확인 — `cell` 직속 메시 21 = 슬래브 1 + 벽 20, 모서리 240 선분, `contents` 22개 무손상. 줄여 볼 때도 이득(전에는 위에서만 안이 보였다) |
| **벽 높이가 배치안에 없다** — `floor.heightMm` 2700 만 있고 실물은 250mm | 실제 맵 위 겹치기 · 정합 눈금 | OPEN | 반투명 윤곽선은 2026-08-06 에 들어갔다(위 행). 남은 것은 **높이 한 필드** — 실물 벽 250mm 가 정합이 맞았는지 읽는 유일한 기준선이라, 2700 으로 그리면 눈금이 안 된다 |
| **터틀봇 라이다가 벽보다 높다** — 250mm 벽을 SLAM 이 못 본다 | 터틀봇 주행 · zones 판정 | OPEN | 주행 문제지 렌더 문제가 아니다. **이중화** — ①벽에 띠를 덧대 라이다 높이 +50mm ②`zones[] keepout` 을 Nav2 마스크로 밀어넣어 라이다가 놓쳐도 안 넘게 |
| **`STACK.md` 464줄 — 초과선 450 (게이트 RED)** | 마감 · 문서 무게 | OPEN | **다른 세션(FR5 트랙)이 87줄을 키웠다.** 처방은 절 이관인데 **옮기는 것은 사람이 확인한다**(D18). 그쪽 세션이 닫을 때 같이 처리한다 — `API-CONTRACT.md` 때와 같은 경우다 |
| **로봇이 안 움직였다** — `setJointsDeg` 를 로드할 때 한 번만 불렀다 | 시연 설득력 · F10 | **CLOSED (2026-08-04)** | 매 프레임 얹는다(`RobotTwin.jsx` 규약) · 팔마다 URDF 하나 · **베이스가 아니라 j1 이 돈다** · 자세는 FK 격자 탐색으로 찾아 손끝 103mm 실측 (`evidence/2026-08-04/arm-motion.md`) |
| **URDF 두 벌의 비용 미측정** — 삼각형 128,888 × 2 | 폰 성능 | **CLOSED (2026-08-04)** | 갤럭시 AR 세션에서 **팔 2대 + 팔마다 다른 애니메이션에 fps 중앙값 90 · 최저 78**. 저해상도 대체 불필요 (`evidence/2026-08-04/global-cam-phone.md`) |
| **소품이 아직 과학실험실 계보다** — 흄후드·글러브박스·의자·잔물건 | 무대 설득력 | OPEN | 방산 라인 장비로 교체 — 조사 결과는 `evidence/2026-08-04/arm-motion.md` 아래 절과 이 표의 다음 행 |
| **두 팔의 충돌을 안 본다** — 같은 자리를 향할 때 | 시연 신뢰도 | OPEN | 화면용 키프레임이라 실기엔 안 쓴다. 실기 궤적이 오면 그때 판정이 필요하다 |
| **팔을 화면에서 못 옮겼다** — 팔 위치가 배치의 변수인데 | F7 편집 · 배치 비교 | **CLOSED (2026-08-04)** | `arms[]` 가 편집 단위. 받침대를 잡아 옮기고 되돌린다 (D60 · 실렌더 31/31) |
| **팔 받침대가 겹침 검사에 안 들어간다** — 팔은 `props` 가 아니다 | 무대 신뢰도 | OPEN | 받침을 소품 위에 겹쳐 세워도 게이트가 조용하다. `layout.sh` 의 바닥 상자 목록에 팔 받침을 넣을지 정한다 |
| **컨베이어 램프에서 탄두가 안 기운다** — 소품이라 수평 그대로다 | 라인 체감 | OPEN | 경사 구간의 `rotDeg` 를 배치안이 들지, 소품이 알아서 할지 정한다 |
| **무대가 반대로 서 있었다** — 작업물이 떠다니고 탄두가 고정이 아니었다 | 시연 설득력 · F10 | **CLOSED (2026-08-04)** | 지그 고정 + 측면 접근 + U자 흐름으로 재설계. `arm` → `arms[]` (D59 · `evidence/2026-08-04/assembly-line.md`) |
| **j6 회전이 글자로만 뜬다** — 손목이 실제로 도는 것은 안 그린다 | 조립 동작의 설득력 | OPEN | 6축 궤적은 시연 녹화가 있어야 한다. 없는 궤적을 지어내지 않는다 (`GOAL-imitation-demo.md`) |
| **컨베이어 위 탄두가 안 흐른다** — 놓인 소품이다 | 라인 체감 | OPEN | series 가 컨베이어 시간을 선언해야 한다. AMR 항목과 같은 결정 |
| **`Shared/data/datasource/` 가 빈 폴더였다** — 문서 2개가 SSOT 로 선언했는데 실체가 없었다 | L3 비교 전체 · 관제화면이 맵편집에 갇힌 원인 | **CLOSED (2026-08-04)** | 계약 5개 + 목업 + **게이트 신설**(주입 2건으로 확인). 화면의 `fetch`·저장소 직접 호출 0건 · 실렌더 17/17 (`GOAL-dash-datasource` · `evidence/2026-08-04/datasource.md`) |
| **관제화면에 시간축이 없다** — `series` 계약은 있는데 소비자가 없다 | F10(와우모먼트) · F8 · V4 되감기 | **CLOSED (2026-08-04)** | `stateAt` 순수 함수 + `setPlayback` 배선 + 게이트(주입 3건 확인) · 실렌더 28/28 (`GOAL-dash-cycle-player` · `evidence/2026-08-04/cycle-player.md`) |
| **팔이 6축 궤적으로 안 움직인다** — 재생은 베이스 요각까지다 | F10 와우모먼트의 본체 | OPEN | **일부러 비워 뒀다** — 없는 궤적을 지어내면 비교 자체가 거짓이 된다. 시연 녹화(`GOAL-imitation-demo.md`)가 오면 `path.js` 의 `createPlayer` 가 같은 재생기에 붙는다 |
| **AMR 이 재생 중 안 움직인다** — series 가 AMR 시간을 선언하지 않는다 | 재생 완성도 | OPEN | 계약(`SHARED-CORE.md` §1.5)에 AMR 사건을 넣을지 정한다. 지금은 지어내지 않고 세워 둔다 |
| **터틀봇 실행 기록의 `layoutId` 가 null 이다** — 배치안↔실주행을 잇는 자리가 비어 있다 | L3 실데이터 · V7 추적 | OPEN | 채우면 팀원 알고리즘 없이도 진짜 지표가 하나 생긴다. `TurtleBot/` 경계라 별도 골 |
| **Dashboard 의 관문 정체 미정** — 화면 중 유일하게 브리지가 없다 | 팀 공유 저장 · 실데이터 합류 | OPEN | ①파일만 ②자기 브리지 ③**FR5·TB 를 읽기 전용 합류(추천)**. 주인님 결정 대기 — 단 사다리 1~4 는 셋 중 무엇이든 모양이 같아 **안 막힌다** |
| 부품을 화면에서 고를 방법이 없다 — 배치안을 손으로 고쳐야 했다 | F7 편집 | **CLOSED (2026-08-04)** | 좌측 팔레트 + 카탈로그 SSOT + 씬 드롭다운 (D52 · `evidence/2026-08-04/palette-editor.md`) |
| **문·창 메시에 선택 표식이 없고 피킹 대상 밖이었다** | 방 껍데기 편집 | **CLOSED (2026-08-04)** | `root` 에 붙어 `contents` 피킹에 안 걸렸다 — 놓아도 다시 못 고치는 상태. `contents` 그룹 + `userData.item` 으로 |
| **팔레트를 손으로 안 눌러 봤다** — 드래그·드롭다운·이름바꾸기 | F7 사용성 | OPEN | 계산·실렌더로만 확인했다. 사람이 눌러야 아는 것들이다 (끄는 느낌·격자 체감) |
| 팔레트를 **폰에서 안 봤다** — 232px 고정폭 | 폰 사용성 | OPEN | 좁은 화면에서 3D 에 남는 폭 미확인. 접히게 할지 정한다 |
| 씬을 **여러 개 만들어 오가는 것**을 안 해 봤다 | 씬 보관함 | **CLOSED (2026-08-04)** | 프리셋 4개를 차례로 만들어 씬 5개까지 쌓이는 것 확인 (`evidence/2026-08-04/editor-ux.md`). 복제·삭제는 여전히 코드만 |
| 빈 방에서 매번 다시 지어야 했다 | F7 편집 | **CLOSED (2026-08-04)** | `새로 ▾` 프리셋 4개 (D56 · `presets.js`) |
| **배치안 겹침을 아무도 안 봤다** — 렌더를 눈으로 보고 통과시켰다 | 무대 신뢰도 | **CLOSED (2026-08-04)** | 3D 상자 게이트 추가. 넣자마자 진짜 결함 5건(컨베이어가 격벽 관통 등) · `workstation` 원점 122mm 치우침 포함 |
| **로봇 팔을 배치 화면에 안 그린다** | F7 · 시연 인상 | **CLOSED (2026-08-04)** | 붙였다. 모듈에 하나만 두고 부모만 갈아 끼운다 · 작업 자세 `setJointsDeg` · 128,808 삼각형 실렌더 확인 |
| **`API-CONTRACT.md` 460줄 — 초과선 450 (게이트 RED)** | 마감 · 문서 무게 | **CLOSED (2026-08-04)** | 배치안·생산성 지표 절을 `LAYOUT-METRICS-CONTRACT.md` 로 **내용 그대로** 이관해 322줄. 로봇 명령과 소비자도 수명주기도 다르다 (`evidence/2026-08-04/fr5-bridge-hardening.md`) |
| **배포 스크립트가 `~/fr5-data/` 를 안 만든다** | 사다리 2 (Teach 지점) | **CLOSED (2026-08-05)** | `rsync` 뒤·`restart` 앞에 `mkdir -p ~/fr5-data/{points,trajectories,slots,history}`. `--delete` 대상이 `~/FR5Web/` 이라 데이터에 못 닿는다. **실기 배포로 생존 확인은 아직** — 다음 현장 세션 |
| **Optimize 패널을 지웠다 — 그래도 비교가 성립하나** | F8·F10 비교 · V7 | OPEN (2026-08-05 · D74) | FR5 가 `run` 을 내보내고 관제화면이 읽는 경계로 갔다. **그 `run` 계약이 아직 없다** — `API-CONTRACT.md` §실행 기록은 사다리 4 착수 때 신설한다. 그때까지 L3·L4 는 목업으로 선다 |
| **궤적의 조건 필드가 계약에만 있다** — `stamp`·`fps`·`startPose`·`endReason`·`dropped`·`purpose` | F10 비교의 정직성 | **CLOSED (2026-08-05 · D74)** | 여섯 전부 구현·검증. 녹화 주기를 폴링의 **절반(15fps)** 으로 내려 `dropped=0` 을 만들었다 — 같은 속도로 적으면 27Hz 실측 스트림에서 결손이 필연이라 `measure` 궤적이 전부 비교에서 빠졌다 (`evidence/2026-08-05/teach-points-trajectory.md`) |
| **지점 이동이 조그 상한(5°)에 막혔다** — 5° 밖 지점은 영원히 못 갔다 | 사다리 2 핵심 기능 | **CLOSED (2026-08-05 · D75)** | 주인님이 실기에서 부딪혔다. **상한을 올리는 대신 경로를 검사**한다 — 5° 간격 보간 표본을 전부 `GetForwardKin`→작업영역 게이트에 태우고 통과하면 `moveJ` 한 번. 조그·일반 moveJ 의 5° 는 **그대로**다(검증이 따로 확인) |
| **긴 이동이 브리지를 FAIL_CLOSED 로 떨어뜨렸다** — `Request-sent` | 실기 조작 전체 | **CLOSED (2026-08-05)** | 파이썬 `MoveJ` 의 `blendT` 기본 `-1.0` 이 **블로킹**이라 33° 이동이 `_guard` 3초 상한을 넘겼고, 버려진 스레드가 xmlrpc 연결을 쥔 채라 이후 전부 오염됐다. **D75 가 만든 게 아니라 드러낸 잠복 버그** — 5° 상한이 가리고 있었다. `blendT=0.0`(논블로킹)으로. 오염된 연결은 이제 "재연결해야 한다"고 사람 말로 보고한다 (단위 5건) |
| **`_guard` 상한을 넘긴 호출은 연결을 영구 오염시킨다** | 실기 조작 안정성 | OPEN (2026-08-05) | 버린 스레드를 죽일 방법이 없어 **재연결만이 복구**다. 지금은 사유를 사람 말로 바꿔 두기만 했다. 어떤 xmlrpc 호출이든 3초를 넘기면 같은 일이 난다 — 호출별 상한을 다르게 줄지 정한다 |
| **`motionQueueLength` 가 "움직이는 중"을 말하지 않는다** — 이동 0.25초 뒤에도 **0** | **조건 6·phase EXECUTING·겹쳐 쏘기 방어** | **OPEN — 감사 1순위 (2026-08-05 실측)** | 33° 이동 직후 로그가 `queue=0 · motionDone=None · servoTarget 전부 0`. 블로킹 `MoveJ` 시절엔 호출 자체가 직렬화해 안 보였는데 논블로킹으로 바꾸며 **그 직렬화가 사라졌고 조건 6 도 안 막는다.** `EXECUTING` 도 안 뜬다. (드리프트 검사는 **D77 로 해소** — 기준을 `lastServoTarget` 에서 우리 `MoveJ` 목표로 옮겼다. `servoTarget` 이 늘 0 인 것은 여기 남은 문제가 아니다.) **추측으로 고치지 않는다** — `robot_state_pkg` 78필드에서 "이동 중" 신호를 실측으로 찾는 게 먼저 |
| **막힌 경로가 실제로 거부되는 것을 못 봤다** | 지점 이동 안전 | OPEN (2026-08-05 · D75) | 표본 논리는 단위 7건으로만. 통과 경로만 실행해 봤고 **중간이 상판을 쓰는 경로를 일부러 만들어 거부시키지 않았다**. 작업영역 실기 거부 항목과 같은 계열 |
| **Teach 가 실기에서 한 바퀴 안 돌았다** | 사다리 2 완료 판정 | **CLOSED (2026-08-05)** | 실기에서 **캡처·녹화·생존·왕복 전부** 확인. 지점 관절이 `/state` 와 소수 4자리 일치 · 궤적 6.196s/93프레임 = 15fps · **dropped 0** · 배포 두 번째에도 생존 · **`home`→`1`→`home` 왕복 성공**(경로검사 7점 통과 · 반복해도 성공 · 주인님 확인). 남은 관측은 아래 큐 항목 |
| **실기가 주인 없이 ARMED 로 서 있었다** | 운영 안전 | OPEN (2026-08-05 관측) | 붙어 보니 `ARMED · 서보 ON · 조종권 admin` — 지난 세션이 DISARM 없이 나갔다. 브리지 재시작이 세션을 닫으며 서보를 내렸지만, **아무도 안 보고 있으면 그대로 남는다.** 조종권 자동 해제는 있는데 **무활동 시 자동 disarm 은 없다** — 넣을지 정한다. **2026-08-05 `/감사` 에서 또 그대로였다**(붙자마자 `phase: ARMED`) — 한 번이 아니라 기본값이다 |
| **정지한 로봇만 녹화해 봤다** | 궤적 품질 | OPEN (2026-08-05) | 실기 녹화는 로봇이 서 있는 6초였다. **움직일 때 결손률은 모른다** — 이동 중에는 xmlrpc 경합이 늘어 결손이 생길 수 있다 (그리퍼 폴링 사고와 같은 계열) |
| **드래그 티칭으로 궤적을 못 찍었다** | 모방학습 시연 수집 | OPEN (2026-08-05) | 저울이 없어 말단 하중 미설정 → 드래그가 막혀 있다. 지금 녹화는 조그로만 확인했다. 말단 하중 항목과 같은 계열 |
| **재생 드리프트를 안 쟀다** — 되감기가 원본과 얼마나 벌어지나 | F10 비교 신뢰 | OPEN (2026-08-05) | 3D 되감기는 눈으로만 봤다. 프레임별 관절 오차를 수치로 내면 "재생이 곧 원본" 을 주장할 수 있다 |
| **경로 고스트가 계약에만 있다** — `POST /preview` 구현 0 | 이동 전 안전 | OPEN (2026-08-05) | 지금은 쌍둥이 **하나를 목표 자세로 갈아 끼워** 미리 본다(화면이 `실물 아님`을 크게 말한다). 경로 선과 반투명 로봇 한 대 추가는 다음. FK 보간이라 역기구학은 안 쓴다 |
| **작업영역(D73)을 3D 가 안 그린다** | 조작 안전 · 거부 사유 이해 | OPEN (2026-08-05) | 상판 사각기둥·벽면을 Live 3D 쌍둥이가 그려야 조그가 왜 거부됐는지 눈으로 안다. 소비자는 **Live** 로 등재했다 (D74 §화면). 별도 골 |
| **비전 제안 고스트를 아무도 안 그린다** | 비전 A축 | OPEN (2026-08-05) | `VISION-CONTRACT.md` 의 `GET /proposals` 가 "화면이 고스트로 그린다" 고 적었는데 소비자가 없다. 소비자는 **Live 하단 스트립**으로 등재. **구현은 툴 좌표계 캘리브레이션 뒤** — 지금은 전부 `toolCalibrationUnverified` 로 거부된다 |
| **우분투 유선 고정이 안 굳는다** — 저장값이 `auto` 로 되밀린다 | 실기 연결 전체 | **CLOSED (2026-08-04)** | `99-robot-lan.yaml`(`dhcp4: false` + `57.10`·`58.10`)로 못 박았다. `method: manual` · 로봇 ping 0.27ms 확인. 네 번 실패한 진짜 원인은 netplan 이 아니라 **붙여넣기 중 들여쓰기가 NBSP 로 바뀐 것**이었다 (`evidence/2026-08-04/fr5-bridge-hardening.md §3부`) |
| **물건 파지를 안 해봤다** — `force` 30% 가 충분한지 모른다 | 사다리 2~4 (pick) | OPEN | 잡을 물건이 미정이다. 올리려면 `SAFETY-RULES.md` §그리퍼 힘 상한대로 **실물로 버티는 최소값을 재서 등재**한다 — 임의로 올리지 않는다 (`evidence/2026-08-04/fr5-bridge-hardening.md §4부`) |
| **3D 손가락 벌어짐 20mm 은 근사** | 3D 신뢰도 | OPEN | 모델명 `PGE A-100-40` 에서 딴 값이고 데이터시트 대조를 안 했다. 실물을 자로 재면 끝난다 |
| **작업영역 게이트가 실기에서 거부하는 것을 못 봤다** | 조작 안전 (조건 12) | OPEN | 거부 논리는 mock 57/57 로만 확인. 실기 첫 판정은 좌표계 대조(0.0mm)까지였고, 지금 손끝이 상판 밖이라 억지로 몰지 않았다. **팔이 상판 위로 갈 일이 생기면 그때 눈으로 본다** (`evidence/2026-08-05/workcell-measure.md`) |
| **작업영역은 손끝만 본다 — 팔꿈치·상완은 판정 밖** | 조작 안전 | OPEN | 벽 여유를 100mm 로 크게 잡아 덮었다. 링크별 판정은 URDF 충돌 모델이 있어야 하고 그건 별도 골이다 (D73 천장) |
| **벽 각도가 부정확하다 (±70mm)** | 작업영역 정밀도 | OPEN | 벽 두 점이 수평으로 91mm 밖에 안 떨어져 상판 길이로 늘리면 오차가 커진다. **벽을 따라 멀리 한 점**을 더 짚으면 확정된다 (`WORKCELL-MEASURE.md`) |
| **`toolCoordId` 를 브리지가 로봇에 넣지 않는다** | 좌표계 · 작업영역 | OPEN | `config.yaml` 에 `0` 이라 적혀 있는데 `SetToolCoord` 호출이 코드에 없고 실기는 `tool 1` 을 쓰고 있었다 (2026-08-05). 작업영역이 `tool1/user1` 에 묶여 있어 **누가 좌표계를 바꾸면 게이트가 거부**하지만, 계약과 구현이 어긋난 것은 그대로다 |
| **`j1` 한계 밖에 갇히면 우리 조그로 못 나온다** | 실기 조작 | OPEN | 2026-08-05 실측 — `j1` 이 `-179.49°` 로 URDF 한계(±175) 밖에 가자 컨트롤러 오류 `3/1`. 그 자세에서는 조그 목표가 전부 한계 밖이라 게이트가 거부한다. **펜던트로만 빠져나온다.** 래치 오류 항목과 같은 계열 |
| **래치된 컨트롤러 오류를 화면에서 풀 길이 없다** | 실기 조작 전체 | OPEN | `ResetAllError` 는 ARM 시퀀스 안에만 있는데 `check_arm` 이 오류가 있으면 ARM 을 거부한다 — **오류를 풀려면 ARM 해야 하고 ARM 하려면 오류가 없어야 한다.** 2026-08-04 그리퍼 작업 중 `8/1` 이 걸려 브리지 재시작·재연결로도 안 풀렸고 펜던트로만 해소 가능했다. 별도 해제 동작을 계약에 넣을지 정한다 |
| **ARM 이 펜던트를 잠그고 되돌릴 길이 없었다** | 실기 조작 · 동료 협업 | **CLOSED (2026-08-05)** | `ARM` 의 `ExitDragTeach`+`SetMode(0)` 이 원인이었고 `DISARM` 이 안 되돌렸다. `mode` 명령 + 헤더 토글로 되돌린다 (D72 · `evidence/2026-08-05/pendant-mode.md`). `check_arm` 의 드래그 구멍도 함께 닫았다 |
| **펜던트와 브리지의 쓰기 경쟁 미측정** | 조작 안전 · 동료 협업 | OPEN | 읽기 공존은 실측됐다(서보 ON·수동 모드 위에 `OBSERVE_ONLY` 성공). **펜던트가 작업 중일 때** `20003` 을 잡으면 어떻게 되는지는 동료 동의를 받고 재야 한다. **D68 로 빈도가 올라갔다** — 팀원이 펜던트를 쓰기 시작했고 이제 같은 랜선·같은 대역(`58.2`)을 나눠 쓴다 |
| 조종권 토큰이 실기에서 왕복하는지 미확인 | V2~ · 그리퍼 | **CLOSED (2026-08-04)** | 실기에서 `claim` → 토큰 발급 → 그 토큰으로 `release` 성공. 어제는 mock 뿐이었다 |
| **폰에는 우클릭 메뉴가 없다** | 폰 사용성 | OPEN | 길게 누르기가 `contextmenu` 로 오는지 미확인(iOS 는 안 올 수 있다). 지금은 `90° 회전`·`삭제` 버튼이 유일한 길 |
| **가려진 물건은 눌러도 앞의 것이 잡힌다** | F7 편집 | **CLOSED (2026-08-04)** | 화면좌표 근접 피킹(20px) + **작은 것만 도와준다**(최대 변 1m 미만). 큰 것끼리 훔치던 부작용까지 수리 |
| **물건을 방 밖으로 끌 수 있다** — 클램프가 없다 | F7 편집 | **CLOSED (2026-08-04)** | 발자국 반쪽만큼 안쪽에서 멈춘다. 단일·다중 한 길 · 좌표를 정수 mm 로 |
| **폰에는 Shift·우클릭이 없다** — 다중 선택·메뉴에 길이 없다 | 폰 사용성 | OPEN | 길게 누르기·두 손가락을 쓸지 정한다 |
| FR5 6MB 를 **폰 회선에서** 받는 시간 미측정 | 폰 사용성 | OPEN | 배치 탭에 들어가면 URDF·STL 을 받는다. 느리면 켜고 끄는 스위치가 필요하다 |
| `localStorage` 용량 한계 미측정 — 씬이 늘면 언젠가 저장 실패 | 씬 보관함 | OPEN | 화면에 말하긴 한다. 팀 공유(D46)로 가면 자연히 풀린다 |
| 무대(배경) 미확정 | 시연 시나리오 작성 | **CLOSED (2026-07-30)** | 과학실험실 · 팔+AMR 배치별 생산성으로 확정 |
| PRD 목표 ↔ 방산 디지털트윈 표현이 서로 안 맞는다 | PRD·무대·완료 판정 | **CLOSED (2026-08-03)** | **①무대 교체 + 목표 유지 + 동작 축(F10) 신설** 로 닫았다 (D51). PRD·MILESTONES·SHARED-CORE·USER-REQUIREMENTS·CONSOLE-REACT·PROJECT-STATUS·SESSION-START 전파 완료 |
| ~~한 사이클의 정의 (처리량의 분모)~~ — PRD 의 유일한 진짜 블로커였다 | F8·F10 비교 전체 | **CLOSED (2026-08-03)** | **1발 = 1사이클** (D51). 무대가 정해지니 분모가 따라 정해졌다. **동사는 D66 에서 조립으로 확정** — 분모는 그대로다 |
| 무대 소품이 아직 과학실험실이다 | S1 · 시연 화면 전부 | **CLOSED (2026-08-03)** | 소품 5개(`conveyor`·`warhead`·`chuck`·`partTray`·`blastWall`) 추가 + `examples.js` 를 해체 라인으로 교체. 규약 5개 전부 통과 · 실렌더 결함 6건 수리 (`evidence/2026-08-03/props-defense-line.md`) |
| **스테이션이 `posMm[2]` 를 버리고 바닥에 그려졌다** — 도달 판정은 3D 로 재는데 | 배치 판정 신뢰 | **CLOSED (2026-08-03)** | `layout-view.js:220` 이 `baseMm ?? 0` 이었다. `baseMm` 은 어디서도 설정 안 된다 — **링은 z=900 기준인데 물건은 바닥에** 있었다. `baseMm ?? posMm[2] ?? 0` 로 |
| `validateLayout` 이 출하 예제에서 실패하고 있었다 — 게이트가 이 함수를 안 불렀다 | 배치안 무결성 | **CLOSED (2026-08-03)** | `scripts/check/layout.sh` 신설 — 스키마·없는 부품 이름·도달 0건·부품 규약(Group·바닥 원점)을 검사하고 `all.sh` 가 자동으로 집어 간다. 부품 이름을 일부러 깨뜨려 FAIL 확인 |
| **창 유리·창틀이 `Z()` 를 건너뛰어 방 밖에 떠 있었다** (D43 거울 사상과 같은 종류) | 배치 3D 전체 · 첫 화면 | **CLOSED (2026-08-03)** | 씬 z 가 `+1.6/+3.6/+1.4` 였다. `scene-axes.sh` 의 **시험용 배치안에 창·문이 없어서** 이미 있던 `b.max.z<0.3` 검사가 헛돌았다 — 픽스처에 창 2·문 1 추가, 버그 재주입으로 FAIL 확인 |
| Dashboard 실화면 미확인 | S1 검증 | **CLOSED (2026-08-03)** | `dev:dash` 실렌더 — 소품 표시·상태줄 스테이션 이름·도달 링 정상 (`evidence/2026-08-03/props-defense-line.md` §5) |
| 컷어웨이가 격벽(prop)을 안 숨긴다 — `updateCutaway` 는 벽만 본다 | 배치 3D 보기 | OPEN | 높이 1200 으로 낮춰 우회했을 뿐 근본은 남았다. 키 큰 prop 을 컷어웨이 대상에 넣을지 정한다 |
| img2threejs 산출물↔우리 JS 규약 | S1 에셋 저작 | **CLOSED (2026-08-03)** | 컨베이어 1개로 확인 — 규약 6개 전부 통과, `Shared/assets/` 불변. **단 파이프라인은 통째로 안 썼다**(20단계·텍스처 정합 중심). 가져온 것은 *참조 옆에 렌더를 놓고 고치는 루프*이고 그게 결함 3개를 잡았다 (`evidence/2026-08-03/conveyor-prop.md`) |
| 소품이 배치 화면 안에서 안 보였다 — 단독 하네스로만 렌더했다 | S1 검증 | OPEN | `LayoutView` 에 컨베이어를 얹어 축척·조명을 본다. 소품 하나에 메시 33개라 **여러 대 깔았을 때 드로우콜**도 같이 잰다 |
| 조립 공정 중 **어디까지를 로봇이 하나** 미정 | 사이클 안의 공정 수 · F10 분모 | BLOCKED | 팀 결정. 데모는 **"신관 결합"** 한 단계로 먼저 선다 (D66 이 해체→조립으로 뒤집었다). 결합 방식은 미사일 장난감 실물 도착 후 판정 — 아래 행 |
| ~~표시(미사일)와 실물(페트병)이 다르다~~ — "화면=실물" 전제가 깨진다 | AR 안전 표시 | **해소 예정 (2026-08-05)** | **실물 소품을 미사일 장난감(불투명)으로 바꾼다** — 화면과 실물이 같아져 D50 의 제약 셋 중 하나가 통째로 사라진다. 실물 도착 후 치수를 재서 메시와 대조하면 닫힌다 |
| ~~**깊이 유효가 불리언이라 24% 와 99% 가 같은 값이다**~~ | 비전 제안 신뢰도 | **CLOSED (2026-08-05)** | `validRatio`·`validRatioCenter` 를 계약에 넣고 **임계값 중앙 50%** 를 실측으로 정했다 — 죽은 상태 0~7.6% / 산 상태 70~85% 로 사이가 비어 있다. Z 65535 포화 클램프도 함께 (D70) |
| **`consts.sh` 가 `CAP_*` 를 실제로 안 본다** — 있는 척하는 게이트 | 문서 무게 신뢰도 | **OPEN (2026-08-05)** | 드리프트 검사가 커맨드/스킬 수와 삼각형 수 둘만 대조한다. 그 사이 `CAP_FOLDER_MD` 가 README 15 · 실제 25 로 벌어져 있었고 아무도 몰랐다(이번에 손으로 발견·정정). `CAP_*` 전부를 `chk` 에 넣는다. **2026-08-05 `/감사` 가 전수로 셌다 — 대조되는 것 5그룹 · 선언만 되고 안 보는 것 10그룹**(`CAP_*` 전부 + `map/make-tags.py` · `map/aim.py` · `assets/make-marker-sheet.py` · `build/config.mjs`) |
| **미사일 장난감의 결합 방식 미정** — 배송 중 (2026-08-05) | 데모 과제 정의 | OPEN | 셋으로 갈린다: ①**돌려 조립** → D50 근거·J6 재파지 사이클 그대로 ②**직선 삽입** → 근거를 "나사산"에서 "정밀 삽입(peg-in-hole)"으로 갱신하고 **J6 재파지 사이클 삭제** ③**얹기만** → 전통 제어로도 되므로 D47·D50 의 "모방학습이 유리하다" 를 재검토. 실물 도착 즉시 판정 |
| `FR5/bridge/` 코드 없음 | F1~F6 전부 | **CLOSED (2026-07-31)** | P0~P2 완성 + 실기 첫 조그 (`evidence/2026-07-31/fr5-first-motion.md`) |
| `web/` 코드 없음 | F1~F4 | **CLOSED (2026-07-30)** | `AR/` · `Dashboard/` 로 갈라 Vite 이관 완료 |
| URDF·STL 복사 | F2 | CLOSED (2026-07-29) | `scripts/assets/sync-from-unity.sh`로 자동화됨 |
| 그리퍼(PGEA-100-40)가 URDF에 없음 | F2 부분 | OPEN | URDF에 그리퍼 링크 추가. 통합형/분리형 STL 중 선택 |
| Fairino 파이썬 SDK가 PyPI에 없어 설치 경로 미확인 | V0 | **CLOSED (2026-07-31)** | 순수 표준 라이브러리 확인 → `fairino_sdk/` 벤더링 (D42) |
| macOS에서 FAIRINO C# SDK 직접 연결 가능 여부 | H0·V0 | **CLOSED (2026-07-31)** | 실물에서 version·6축·TCP readback 성공. Python SDK는 별도 갭 (`evidence/2026-07-31/fr5-live-readback.md`) |
| 같은 FR5라도 다른 개체 배정 시 IP·펌웨어를 고정값으로 오인 | V0~V7 | **CLOSED (2026-07-31)** | profile+preflight 구현, 실측 모델 `FR5-V1-002(V6.0)` 등재 |
| 어댑터 xmlrpc 호출 무타임아웃 — stop/disarm 행이면 잠금 보유 채 브리지 전체가 굳는다 | V0~V2 | **CLOSED (2026-08-03)** | `_guard()` 스레드 상한 3초 + **stop 은 잠금 0.2초만 기다리고 무락 발사** (SAFETY-RULES 제3원칙) · disconnect 도 잠금 안으로. D45 |
| 실기 게이트 — 2클라이언트 409 · 10분 폴링 · 재부팅 자동기동 | V0~V2 | **CLOSED (2026-08-03)** | 셋 다 통과 (`evidence/2026-08-03/fr5-field-gates.md`). 단 프레임 공백 5.4초 1회 관측 |
| 그리퍼 SDK 시그니처·실물 정체 미확인 | P3 | **CLOSED (2026-08-03)** | 실물 PGE A-100-40 · `SetGripperConfig(4,0)` · 개폐 2회 실기 확인 (`STACK.md` §그리퍼) |
| 조종권이 이름 문자열만으로 통과한다 (hello 위장) | V2~ · P3 그리퍼 | **CLOSED (2026-08-04)** | claim 이 1회용 토큰 발급, hello·arm·disarm·release·disconnect 가 대조 (D55). 로그인 없이 닫았다 |
| 그리퍼 지령 pct ↔ 읽기 pct 방향 반대 · `GetGripperMotionDone` 필드 순서 의심 | P3 | OPEN | 사다리 1 착수 때 3점 이상 대응표 실측 → 브리지 한 곳에서 변환 (하드 룰 5) |
| **컨트롤러 충돌 감지가 켜져 있지 않다** — 게이트 조건 4·5 의 전제 붕괴 | 안전 전체 | **부분 CLOSED (2026-08-04)** | 브리지가 ARM 마다 설정을 넣고 되읽기 대조 (조건 26 · `evidence/2026-08-04/fr5-bridge-hardening.md`). **단 실제로 반응하는지는 미검증** — 아래 항목 |
| 충돌 민감도 실측값 미정 — SDK 등급(1~10)과 매뉴얼 N/m 은 축이 다르다 | 안전 전체 | OPEN | 보수적 값에서 한 단계씩 낮추며 실기 실측. STOP 손에 두고 |
| **충돌 감지가 실제로 반응하는지 미검증** — 등급 5 를 넣었을 뿐이다 | 안전 전체 | OPEN | 사람이 팔을 밀어 서는지 확인. 이번 변경의 진짜 완료 판정 |
| 말단 하중·설치 방향 미설정 — 드래그·충돌감지 오작동 조건 | 안전 · 모방학습 | **부분 CLOSED (2026-08-04)** | 하중 0.6kg·무게중심 실기 적용 후 **되읽기 일치 확인**. **단 그건 그리퍼만의 값이다** — 아래 항목 |
| **카메라를 얹으면 말단 하중이 다시 틀린다** | 안전 · 모방학습 | OPEN | D435+브래킷이 그리퍼 폭 **밖**으로 나와 무게중심이 툴 축 위에 없다. 질량만 고치고 `cogMm` 오프셋을 빠뜨리면 **드래그 티칭·충돌 감지가 조용히 망가진다**(조건 4·5·25). 장착 후 카메라+브래킷 실측 무게로 재설정 (D61). **오프셋 수평 성분은 82mm 로 확정**(D69) — 남은 미지수는 질량뿐 **무게중심 수평 오프셋 82mm 은 확정**(D69) — 남은 건 질량뿐인데 **2026-08-05 현재 저울이 없어 못 잰다.** 이것 하나가 드래그 티칭을 막고 있다 |
| **파지점이 카메라 화각 밖으로 나간다** — 렌즈가 툴 축에서 82mm 옆 | 모방학습(B축) | **OPEN (2026-08-05)** | 손끝 높이에서 광축 이탈각 45.7° > 깊이 반화각 44.87°(실측). 컬러(70.43°)가 깊이(89.74°)보다 **먼저** 눈을 감는다 — 손끝 아래 3.6cm vs 깊이 Min-Z 한계 2.5cm. **A축 블로커는 아니다**: 앵커를 뜨는 15.6cm 에서는 장착 방향이 어느 쪽이든 화면 안이다(D70). "파지 순간 RGB 만"은 기각, 앵커 봉인이 유일 수단 |
| ~~**브래킷 틸트가 있나**~~ | 비전 A축 | **CLOSED (2026-08-05)** | **틸트 없음 확정.** 그리퍼에 장착한 상태로 찍은 깊이 848×480(89.74° — 최대 화각) 프레임에 그리퍼가 **0** 이다. 손가락은 카메라에 강체로 고정돼 있으므로 다른 해석이 없다 (`evidence/2026-08-05/cam-fov.md`) |
| **손목캠이 접촉 구간을 못 본다** — 23° 틸트 재출력 여부 | 모방학습(B축)만 | OPEN | **A축 블로커가 아니다** — 앵커 봉인이 이미 "마지막은 안 본다"는 설계고 그리퍼 개폐는 실시간 필드로 읽는다(D65). B축도 표준 구성이 손목캠+오버헤드라 오버헤드(폰 글로벌 카메라)로 먼저 시도한다. **부족이 실제로 드러나면** 재출력 — 그때 80/82mm 재측정과 말단 하중 재설정이 따라온다 (D69) |
| **툴 0 에 이미 Z 135mm 오프셋이 들어 있다** — 누가 어떤 근거로 넣었는지 모른다 | TCP 표시 정확도 | OPEN | 실측(2026-08-04). 계약의 "TCP 는 플랜지 기준" 서술이 이 개체에선 틀렸다. 그리퍼 실측과 대조해 맞으면 등재, 아니면 재캘리브레이션 |
| Power Detection(출력 상한) 미설정 — SDK `SetPowerLimit` 되읽기 없음 | 접촉 충격 | OPEN | 값 근거를 못 찾았다. 충돌 등급 실측 후 함께 판단 |
| **FR-HMI(3위치 인에이블) 부재** — 컨트롤러 간섭영역·안전정지가 Enable 실패 가능 | 위치 안전망 | OPEN | 컨트롤러 위치 안전망을 방어선으로 세지 않는다. URDF 소프트리밋이 유일 (`SAFETY-RULES` §FR-HMI) |
| 부팅 후 로봇 자동 재연결 없음 + 그 사실을 팀이 알 방법 없음 | 운영 | OPEN | 서비스 기동 후 지연 `/connect` 1회 자동 시도(실패해도 무해) 또는 화면에 상시 표시 |
| 브리지 비정상 종료 시 컨트롤러 세션 점유 — 복구 힌트 없음 | V0 | OPEN | `/connect` 실패 사유에 "컨트롤러 세션 점유 의심 — 재부팅" 힌트 추가 |
| 브리지 로그가 `journalctl`(ssh 보유자)에게만 보인다 | 운영 | OPEN | 최근 N줄 링버퍼 + `/debug/log` 읽기 전용 (LAN 팀신뢰 전제) |
| 유지보수 정비분의 **실기 미반영** — 마감 시점 우분투가 응답 없음 | 운영 | OPEN | PC 켠 뒤 `/우분투` → `bash scripts/deploy/fr5-ubuntu.sh`. 코드는 커밋됨 |
| 토큰 도입 후 **실기 조종권 왕복 미확인** — mock 으로만 봤다 | V2 | OPEN | 실기에서 claim→arm→disarm→release 한 바퀴 |
| 모방학습 — 뎁스카메라 스트림의 **계약** 미정 | 신규 요구 | **CLOSED (2026-08-04)** | **계약 신설** (D61) — 카메라는 `LAYOUT-METRICS-CONTRACT.md` §카메라, 제안은 `API-CONTRACT.md` §제안. `minZmm`·`valid`·찍은 시각을 서버가 싣고, 제안은 승인 시 `moveJ`·`gripper` 로 번역해 같은 게이트를 다시 탄다 |
| 뎁스카메라 관문을 **어느 기계에서 돌리나** | 모방학습 · 비전 | **재개 OPEN (2026-08-04)** | D48 의 "주인은 파이" 를 보류로 되돌렸다. 후보 넷(손목 파이 / 밑동 파이 / 우분투 직결 / PoE)이 전원·비틀림·시계·발열에서 다르게 걸린다. **전원 경로가 실물 미확인** — 배선 실물 확인 후 결정 (D61) |
| 서보 스트리밍 경로 없음 — `MoveJ` 로는 정책의 초당 수십 프레임을 못 받는다 | 모방학습 전체 | OPEN | `ServoMoveStart`/`ServoJ`(8ms)/`ServoMoveEnd` 로 브리지에 스트리밍 모드 신설 (D47 · STACK 등재됨, 실기 미검증) |
| 스트리밍용 안전 게이트 미구현 — "명령당 5°" 는 점 대 점 전제다 | 모방학습 안전 | OPEN | 각속도 ≤30°/s · 경과시간 비례 · 프레임 결손 3회면 종료 (`SAFETY-RULES.md` §서보 스트리밍 상한) |
| **`TCP_NODELAY` 를 아무도 안 켠다 — 추측이다, 아직 안 쟀다** | 서보 스트리밍 지연 | OPEN (2026-08-06) | SDK·브리지 전부에 `setsockopt` 0건. Nagle 이 켜져 있으면 작은 프레임을 모았다 보내 최대 수십 ms 를 붙잡는다. **단 파이썬 `xmlrpc.client` 가 이미 켜고 있을 수 있어 원인이라고 단정 못 한다** — 서보 경로 착수 시 ①실제로 꺼져 있나 ②주기 지터에 얼마나 기여하나를 재고, 그 뒤에야 값을 문서에 박는다 (`GOAL-servo-stream.md` 천장 항목과 같은 줄) |
| 텔레오퍼레이션 입력 장치 없음 — ±1° 버튼으로 30~50회 시연은 불가 | 시연 수집 | OPEN | **먼저 직접교시로 우회 가능한지 본다**(아래 행) — 되면 이 갭은 안 사도 닫힌다. 안 되면 게임패드·3D 마우스를 서보 스트리밍 경로에 연결 |
| **직접교시가 실기에서 되는지 미검증** — `DragTeachSwitch` 는 SDK 에 있다(`Robot.py:943`) | 시연 수집 (1순위 후보) | OPEN | 킬실험 30분 — ①걸었을 때 팔이 사람 힘으로 밀리나 ②그 동안 20004 관절값이 따라오나. **선행: `SAFETY-RULES` 조건 25**(드래그 중 stop 외 차단) |
| 드래그 티칭 중 원격 명령 차단이 화면 힌트뿐 — 서버 게이트가 없다 | 직접교시 안전 | OPEN | `SAFETY-RULES` 조건 25 구현. 사람이 팔을 잡고 있는데 원격 조그·arm 이 들어오는 경로를 서버가 막는다 |
| 그리퍼 `force` 상한 미설정 — SDK 기본값에 기대고 있다 | 뚜껑 잡기·체결 안전 | OPEN | 실물로 잡는 힘 하한(뚜껑을 놓치지 않는)과 상한(변형 안 되는)을 재고 `STACK.md` 에 등재 |
| ~~손목 뎁스카메라 미확보~~ | 정책 학습 | **CLOSED (2026-08-04)** | **D435 실물 확보** (S/N 254522075185 · 펌웨어 5.17.0.10 · 맥에서 열거 확인). 그리퍼 상단 장착 · 해상도별 Min-Z 는 `DEPTH-CAM.md` (D61) |
| 학습용 GPU 미확보 | 정책 학습 | BLOCKED | 예산·확보는 팀 결정. 인식→제안 축은 CPU 로 되므로 **A 축 착수는 막지 않는다** |
| ~~**USB2 로 붙어 있다**~~ — 848×480 이 10Hz 에서 막힌다 | 모방학습 | **CLOSED (2026-08-05)** | `Usb Type Descriptor 3.2` 로 재확인. 깊이 848×480 **90Hz** · 424×240 **90Hz** · 컬러 848×480 60Hz 가 전부 열렸다 — 정책이 요구하는 10~50Hz 를 넘는다. 케이블은 B축 선행 조건이 아니게 됐다 (`evidence/2026-08-05/cam-fov.md`). **호스트를 옮기면 `lsusb -t` 로 `5000M` 를 다시 확인한다** |
| `/state` 간헐 JSON 깨짐 (char 521 부근, 전이 순간) | V0·V1 | **CLOSED (2026-08-03)** | 브리지 무죄 — 개발 맥의 rtk 훅이 파이프 출력 510B에서 잘라 `[full output: …]` 꼬리를 붙인 것. 파일 저장·`rtk proxy curl` 은 100% 정상 (95회 실측) |
| 실기 P2 잔여 게이트 — 2클라이언트 409·10분 폴링 | V2 | OPEN | 다음 현장 세션에서 각 5분 |
| `fairino_cs/`(폐기된 C# 경로) 보존 여부 | — | OPEN | 증거 보존 중 — 다음 정리 때 archive 이관 판단 |
| 씬 축 매핑이 거울이었다 (`planToScene` det −1) | 3D 전체·AR 오버레이 | **CLOSED (2026-08-02)** | D43 · 17곳 수정 + `check/scene-axes.sh` 게이트 (`evidence/2026-08-02/cam-overlay.md`) |
| 글로벌 카메라 캘리브레이션 도구 없음 | AR 오버레이 | **CLOSED (2026-08-02)** | `scripts/map/` 5개 + 게이트 3개. 합성 검증 0.83px |
| 카메라 스트림을 어느 브리지가 소유하나 | G4 라이브 영상 | **CLOSED (2026-08-03)** | 손목 카메라는 파이(D48). **고정 글로벌 카메라는 별개** — 그건 아직 미정이다 |
| 렌즈 왜곡 언디스토트 경로 없음 | AR 오버레이 정합 | OPEN | three.js 는 핀홀만 그린다. k1 −0.1 이면 모서리 97px. 실측 k1 을 재고 영상 언디스토트를 넣는다 |
| **글로벌 카메라 하드웨어 미확보** | AR 오버레이 전부 | OPEN | 있는 USB 웹캠은 **640×480 고정**(3코덱 실측) → 실제 맵에서 1.9 px/칸, 안전선의 1/3. 태그로 못 메운다. 1080p 이상 필요 — 갤럭시 임시 가능. **잠글 것 5개: 초점(AF)·노출(AE)·HEIF·손떨림보정·삼각대 강체 고정.** 폰 앱 설정은 우리 코드가 못 잠근다(사람이 한다). 촬영 중 상시충전은 스웰링 위험 (`evidence/2026-08-03/global-cam-aim.md`) |
| 카메라 자리를 계산으로 못 정한다 (화각↔캘리브 순환) | 카메라 설치 | **CLOSED (2026-08-03)** | `map/aim.py` — 실시간 px/칸 판정. 정확한 투영 대비 2% 이내 |
| 높이 정합이 사진으로 검증 안 됨 | AR 오버레이 | OPEN | 합성 사진에 높이 있는 실물이 없다. 실제 맵에서 벽을 세우면 닫힌다 |
| 컷어웨이가 벽만 숨기고 문짝·문틀은 남긴다 | 배치안 3D 보기 | OPEN | 숨긴 벽의 문 유리가 허공에 뜬다. `updateCutaway` 가 문 부품도 같이 토글해야 한다 |
| `mapToLab` 측정 절차 미정 | AMR↔FR5↔AR 연결 전부 | OPEN | TB-CONTRACT §미래접점③ 이 "P4 에서 정한다"로만 남아 있다. **이번 트랙 최대 관문** |
| `docs/evidence/2026-07-31/` 15건 > 상한 14 | 게이트 레드 | **CLOSED (2026-08-03)** | tb 검증 mock+실기 두 파일을 `tb-mock-verify.md` 한 건으로 합쳐 14 로 |
| **tb-bridge 와 FR5 브리지가 같은 포트 5055 를 적었다 — 기계는 한 대다** | 터틀봇 관제 전부 (로봇 전원과 무관) | **CLOSED (2026-08-06 · D80)** | tb 를 **5056** 으로. 화면은 0줄 고쳤다 — `http.js` 가 `location.host` 만 써서 포트가 애초에 클라이언트에 없었다(D29 덕). 포트 정본을 `config.yaml` 로 세우고 기동 스크립트 둘이 그 한 줄을 읽는다 — **원래 있던 `port:` 는 아무도 안 읽는 죽은 필드였다.** 사본은 vite dev 프록시 하나뿐이고 `check/consts.sh` 가 잡는다(로직 5케이스 자가검증). 실측 — 5056 기동·프록시 200·`tb-bridge-verify` 20/22 |
| ~~`tb-bridge-verify` 2건이 빨갛다~~ | 터틀봇 관제 화면 신뢰 | **CLOSED 2026-08-06 · 22/22** | **화면이 안 채우는 게 아니었다** — 원인이 서로 달랐다. ①**로그 백로그**: 버퍼도 재생도 멀쩡한데 **브리지가 기동 때 아무것도 안 남겨** 버퍼가 비어 있었다. 갓 뜬 브리지의 로그 패널이 빈 채로 열려 *"로그가 안 오나"* 와 *"아직 아무 일도 없었나"* 를 못 가른다 → 기동 한 줄(`adapter=` 포함 · SR_24)을 남긴다 ②**기록 목록**: 빈 상태 행(`기록이 없어요`)도 `tbody tr` 이라 `행 ≥ 1` 대기가 **그 자리에서 통과**했고, 목록이 채워지기 전에 badge 를 읽었다 → 행에 `runs-empty` 표식 + 검증은 `:not(.runs-empty)` 를 기다린다 |
| tb-bridge 실기 배포 (우분투 real 어댑터) | 터틀봇 실기 관제 | **CLOSED (2026-08-03) · 주소 갱신 2026-08-06** | 기동·실렌더 6/6 (`evidence/2026-07-31/tb-mock-verify.md` §P4). **호스트는 `ej@192.168.11.2` → `ej@192.168.30.240`** — WiFi 교체로 바뀌었고 FR5 와 같은 기계다(D63 고정 주소). 코드는 `~/fr5tb/TurtleBot/` |
| 로봇 실주행 (bringup·teleop·SLAM) | 터틀봇 실주행 전부 | **BLOCKED** | WiFi 는 교체됐다(30 대역). **2026-08-06 실측 — 도메인 0·1·2·30 전부 토픽 0개**(`/rosout`·`/parameter_events` 뿐)라 로봇이 여전히 안 켜져 있다. 켜면 `config.yaml` 의 `ros_domain_id: 2/1` 을 실기와 대조한다(스택가드 미통과 값) |
| 실물 그리퍼·tool/user·페이로드·충돌 형상 미보정 | V3·V5·V6 | BLOCKED | 현장 값 확정 전 시뮬레이션 후보를 “최적” 또는 실기 승인으로 승격하지 않는다 |
| 폰 HTTPS 접속 방식 미결정 (mkcert vs 터널) | V3 | BLOCKED | 개발 환경과 시연 환경에서 접속 방법 확정 |
| 기록 저장소 미정 (파일/SQLite/Supabase) | F6, V4 | **CLOSED (2026-08-06)** | **D81** — 로컬 파일이 정본, 공유층은 랩 안 SQLite. 클라우드는 랩 밖 열람 요구가 생길 때. **보존 기간은 별개로 아직 미정**(팀 회의) |
| v3 기능이 실제 동작하는지 미검증 — 이름만 확인. V3 씬은 빌드에 없음 | F1·F3 | OPEN | 옮길 기능마다 원본 C# 직접 읽기. 안전 로직 우선 (`docs/evidence/2026-07-29/v3-feature-survey-limits.md`) |
| SDK 상태 패킷이 안전조건을 주는지 | V0 전체 | **CLOSED (2026-07-29)** | 킬-실험 통과 — 4개 값 전부 확인. `docs/ref/contract/SAFETY-RULES.md` |
| 비상정지 신호의 SDK 필드명 | 안전 전체 | **CLOSED (2026-07-30)** | `EmergencyStop` 외 9개 확인. `docs/evidence/2026-07-30/sdk-state-fields.md` |
| **편집한 배치안이 저장되지 않는다** — 새로고침하면 사라진다 | L1 · L3 비교 | **CLOSED (2026-07-31)** | `localStorage` 저장 + 되돌리기 한 단계. 실렌더 9항목 확인 (`evidence/2026-07-31/dashboard-l1-editor.md`). **천장 — 브라우저 한 대 안에서만 산다.** 팀 공유는 아래 항목 |
| 저장이 **브라우저 한 대 안에만** 있다 — 팀·기기 간 공유 불가 | L3 비교 · 팀 검토 | OPEN | `Shared/data/config/` 슬롯으로 올린다 (이관 H 단계) |
| `pointerup` 이 유실되면 편집기가 멈춘다 (`pointercancel` 미청취) | L1 사용성 | OPEN | **합성 이벤트에서만 재현됐다.** 실제 입력은 `setPointerCapture` 가 막는다 — 실사용에서 나오면 그때 고친다 |
| 격자 사이 좌표를 못 넣는다 (끌기가 100mm 에 붙는다) | L1 · 실측 치수 반영 | **CLOSED (2026-07-31)** | x·y·회전 숫자 입력. 실렌더 11항목 (`evidence/2026-07-31/dashboard-l1-editor.md`) |
| `userData.item` 이 `{kind,id,type}` 만 담아 **회전을 안 실어 나른다** | 3D 노드에서 값을 읽는 모든 화면 | OPEN | 지금은 **배치안에서 직접 읽어** 우회했다. `Shared/view3d/parts.js:162` 를 고치면 근본이 닫힌다 |
| **편집기를 폰에서 안 봤다** — 패널 접힘 · `SLOP_PX=4` 가 손가락에 맞나 | L1 폰 사용성 | **CLOSED (2026-07-31)** | 주인님이 폰에서 확인 — "잘됨". 자동화로는 세 번 다 막혔던 항목이다. **기종·수치는 안 남겼다** — 문턱을 다시 건드리면 그때 재확인한다 |
| 문서 무게 경고 — INDEX 54행(45) · PROJECT-STATUS 160(120) · DECISION-LOG · 07-31 evidence 10건(8) | 진입 비용 | OPEN | 2026-08-03 에 낡은 표기 정리 + dashboard 증거 5→1 통합으로 일부 줄였다. 나머지는 soft 경고(게이트 통과) — **옮기는 것은 사람이 확인하고 한다**(D18) |
| **고르기만 해도 편집이 생긴다** — 클릭 중 손이 1px 만 움직여도 격자로 스냅해 커밋 (`runN1` y 7620→7600) | L1 · 배치안 신뢰 | **CLOSED (2026-07-31)** | 끌기 문턱 `SLOP_PX = 4` 를 뒀다. 넘기 전에는 **메시도 데이터도 안 건드린다** (`interaction.js`). 배포본에서 확인 — 클릭 시 편집 0건, 끌기는 그대로 |
| 끌기가 끝나도 **미리보기 모드(`live`)가 안 꺼져** 패널이 놓은 자리에 얼어붙었다 | L1 · 배치안 신뢰 | **CLOSED (2026-07-31)** | 되돌리기를 해도 옛 좌표를 보여줬다. 커밋할 때 `live` 를 끈다 (`LayoutView.jsx`). 배포본에서 `6300,8000` → ⌘Z → `2600,7500` 확인 |
| 메인뷰가 **되는 기능을 "다음 단계"라고 적어 뒀다** | L1 사용성 | **CLOSED (2026-07-31)** | 문구 전면 교체 + 실렌더 확인 (`evidence/2026-07-31/dashboard-l1-editor.md`). 판정 `ready-for-review` — 실제 폰은 아직 |
| 벽·문·창을 클릭해도 안 골라지는데 **화면이 이유를 안 알려준다** | L1 사용성 | OPEN | 의도된 제한이다(`userData.item` 없는 노드). 지금은 고치지 않고 기록만 — 실사용에서 오해가 나오면 그때 만든다 |
| Vercel 이 모노레포 workspaces 를 못 빌드한다 (하위 폴더 `npm install` 이 `@fr5/shared` 를 못 푼다) | 배포 자동화 | OPEN | 지금은 로컬 빌드 산출물 업로드 (D24). 루트 기준 설정은 AR 과 충돌하므로 보류 |
| `Dashboard/dist` 7.2MB 중 6.6MB 가 **아직 안 쓰는** URDF·그리퍼 STL | 배포 속도 | OPEN | `publicDir` 이 `Shared/assets` 전부를 복사한다. L2 에서 팔을 세우면 쓰인다 — 그때까지 방치 |
| Dashboard 기존 CSS가 새 의미 토큰을 아직 안 씀 (`--c-ok` 선택 탭 · 밝은 화면의 `color-scheme: dark`) | Dashboard·FR5 시각 통일 | OPEN | 다음 UI 구현 때 `--c-selected`·밝은 color-scheme으로 교체. 이번 변경은 문서·토큰까지만 |
| 터틀봇 상판 마커 크기가 검출 한계인가 | AMR 위 가상 팔 | **CLOSED (2026-07-31)** | 35mm·1.8m·100% 확인. 단 `src=1280&cv=960` 필수 (`evidence/2026-07-31/marker-live-phone.md`) |
| **고해상도에서 fps 13~14** — 시각적으로 끊긴다 | AR 체감 품질 | OPEN | 검출 캔버스를 줄여도 그대로다 → 병목이 카메라 디코딩 쪽. 프레임 건너뛰며 검출하는 방식 검토 |
| **고정 카메라 경로가 아직 없다** — 맵 전체를 한 대로 찍고 AMR 위에 팔 | 시연 본편 | OPEN | 마커가 아니라 **호모그래피 4점 + 위치값**. 맵·카메라 위치 확정 후 착수. 색 검출은 사진 1장으로 먼저 판정 |
| AMR 위치(odom·AMCL)를 받을 수 있는지 미확인 | 고정 카메라 · 하이브리드 | BLOCKED | 팀원 ROS 쪽 확인 필요. 못 받으면 영상 색 검출로 우회 |
| **AR 을 영상으로 남길 방법이 없었다** — 폰 내장 녹화는 버튼바까지 찍힌다 | 시연 · 증거 | **CLOSED (2026-07-31)** | ⚙ 에 [● 녹화]. 카메라+겹침만 합쳐 mp4. 합성 레이어를 픽셀로 확인 (`evidence/2026-07-31/ar-record.md`). 판정 `ready-for-review` |
| 녹화가 **진짜 카메라 영상과 합쳐지는지 못 봤다** — 가짜 카메라 캔버스로만 확인 | AR 녹화 신뢰 | OPEN | 자동화 탭에 웹캠이 안 붙는다. **폰에서 한 판 찍어 본다** — 그때 iOS 사파리 코덱도 같이 본다 |
| 녹화 중 **fps 비용 미측정** — 프레임마다 한 장 더 그린다 | AR 체감 품질 | OPEN | 이미 13~14fps 다. 폰에서 `[측정 30초]` 를 녹화 전후로 돌려 차이를 잰다 |
| **실기가 낸 값이 기본값이 아니었다** — 마커 `#2`(실물은 `#5`) · AR.js 기본 해상도(58%) | AR 전체 | **CLOSED (2026-07-31)** | 배포본을 폰에서 열었더니 로봇이 안 떴다. `.env` 를 `#5`·45mm 로, `ar.js` 기본을 `src=1280&cv=960` 으로 (D42). 배포본에서 확인 |
| `FR5_MARKER_MM` 하한이 **설정 40 · 슬라이더 10** 으로 서로 달랐다 | 크기 시험 | **CLOSED (2026-07-31)** | 시트의 10~35mm 를 설정으로 못 박았다. `config.mjs` 를 10 으로 맞췄다 (D42) |
| **AR 새 배포본을 폰에서 안 봤다** — 마커 #5·새 기본 해상도 | AR 시연 | OPEN | `https://fr5ar.vercel.app/ar.html`. 로봇이 뜨는지 · 녹화본에 카메라 배경이 깔리는지 · fps 를 본다 |
| **연속 스크럽으로 잰 관절 값에는 누적 지연이 섞인다** — 각속도 제한(70°/s)이 직전 자세에서 출발한다 | 실렌더 판정 신뢰 | **CLOSED (2026-08-04)** | 1초 간격 전수가 결합 거리를 445~712mm 로 냈고 하마터면 로봇 결함으로 적을 뻔했다. 직전 시각을 불리하게 주고 다시 재니 **전부 24~98mm 로 수렴**. 시각마다 독립으로 안정시켜 잰다 (`evidence/2026-08-04/assembly-line.md`) |
| **세로 창에서 세로 점유가 0.283** — 45° 대각에서 이 방은 넓고 낮아 세로 캔버스를 못 채운다 | 폰·세로 화면 | OPEN | 잘리지는 않는다(가로 0.935 가 빡빡한 축). 방위각·부감을 창 비율에 따라 바꾸면 회수되지만 **시점 선택**이라 프레이밍 수리 범위 밖으로 뒀다 |
| **실렌더 게이트가 자기 크래시를 삼켰다** — 11·12 절이 안 도는데 31/31 초록 | 게이트 신뢰 | **CLOSED (2026-08-04)** | `finally` 의 `process.exit` 이 예외보다 먼저 돌았다. `catch` 로 FAIL 을 적게 했다. 드러난 뒤 45개 중 44개 (`dash-web-verify.mjs`) |
| **시점이 세로 화각만 봤다** — 가로 화면에서 방이 화면의 13.6%, 세로 화면에서 양옆 40% 잘림 | 심사·시연 인상 | **CLOSED (2026-08-04)** | AABB 8모서리를 카메라 축으로 분해해 두 화각 동시 맞춤 + 겨눔점 재중심 + 비율 변화 시 재맞춤. 13.6% → 59.8%. 게이트 §13 에 하한을 박고 **원래 버그를 주입해 FAIL 을 확인**했다 |
| **자세표가 실기 한계 밖 각을 들고 있었다** — `unscrew.j6 = 300°` (한계 ±175) | 화면이 실기를 흉내내나 | **CLOSED (2026-08-04)** | `urdf-loader` 가 말없이 잘라 증거 문서에 **한계에 붙은 값**이 자세표 값인 양 적혀 있었다. `limits.js` 사본 + **게이트가 URDF 원본과 대조** (`motion.sh`) |
| **폰에서 관절 링을 안 잡아 봤다** — 링 크기가 월드 고정이라 줌아웃하면 작아진다 | 폰 사용성 | OPEN | 화면 픽셀 기준 보정 미적용. 폰에서 한 판 잡아 보고 필요하면 카메라 거리에 비례시킨다 |
| **`fetch` 에서 조립팔이 대기 자세로 선다** — 화면 글자는 "신관을 가지러 간다" | 시연 설득력 | OPEN | 옛 전역 표에 `fetch` 칸이 없던 것을 사건으로 **그대로 옮겼다**(동작 불변이 이관 판정이었다). `scenario/presets.js` 두 줄이면 고쳐지고, 고치면 FK 를 다시 재야 한다 |
| **사람이 만든 자세의 충돌을 안 본다** | 실기 이관 안전 | OPEN | 화면 저작이라 실기로 안 나간다(`timeline.sh` ⑨). 실기 티칭(`/points`)이 붙을 때 같이 본다 |
| **빈 방에 조립 라인 사건 13개가 딸려 왔다** — 자리 6개를 다 놓기 전엔 아무것도 안 돌았다 | 빈 방에서 짓기 | **CLOSED (2026-08-04)** | `getScenarios()` 가 프리셋을 하나만 냈고 해결기가 프리셋 id 를 몰랐다. 빈 방 → `blank`(사건 0개) · 배치안이 `scenarioId` 를 따라간다 (`scenario.sh` ⑤-c) |
| **경로를 그려도 시나리오에 넣는 길이 화면에 없었다** — 같은 질문을 세 번 받았다 | 사용성 | **CLOSED (2026-08-04)** | 답은 "사건에 AMR 을 붙인다" 인데 `[경로 끝]` 이 아무 말도 안 했다. `[시나리오에 넣기]` 가 사건을 만들고 시나리오 탭으로 데려간다 (`dash-web-verify` §25) |
| **번호 라벨 텍스처가 GPU 에 쌓였다** — 점을 한 번 끌 때마다 점 개수만큼 | 폰·장시간 편집 | **CLOSED (2026-08-04)** | `CanvasTexture` 는 지오메트리 정리로 안 없어진다. 실측 10→50 → 뷰 dispose 가 기즈모를 지운다 → 10→10 |
| **모르는 카드 종류가 NaN 지오메트리를 만들었다** — 보이지 않는 깨진 물건이 남는다 | 팔레트 확장 | **CLOSED (2026-08-04)** | `place()` 가 마지막 분기로 흘러갔다. 모르는 종류는 만들지 않고 화면이 말한다 |
| **`+ 사건 추가` 로 만든 사건은 자리가 비어 있다** — 그대로 두면 그 구간에 아무 일도 안 일어난다 | 사용성 | OPEN | 시나리오 탭에서 자리를 골라야 한다. 직전 사건의 자리를 물려받게 하면 닫힌다 |
| **폰에서 팔레트가 232px 고정** — 500px 화면의 46%를 먹어 자세·경로 편집이 좁다 | 폰 사용성 | OPEN | 자세 패널은 2열·34vh 로 줄였고 편집 중 겹치는 판은 접었다. 근본은 팔레트 접기 |
| **경로 편집 중 AMR 을 다른 배치안으로 옮기면** 어떻게 되는지 미확인 | 편집 안정성 | OPEN | 배치안 전환 시 기즈모가 떨어지는지 안 봤다 |
