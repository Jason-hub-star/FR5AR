분류: **SSOT**. 스펙과 구현 사이에 남은 갭을 추적하고, 다음 행동을 본다.

# GAP-MATRIX — 갭 추적

> 갭이 채워지면 상태를 `DONE`으로, 진행 중이면 `IN_PROGRESS`로, 팀 회의 대기면 `BLOCKED`로 바꾼다.

| 갭 | 영향받는 기능 | 상태 | 다음 행동 |
|---|---|---|---|
| 부품을 화면에서 고를 방법이 없다 — 배치안을 손으로 고쳐야 했다 | F7 편집 | **CLOSED (2026-08-04)** | 좌측 팔레트 + 카탈로그 SSOT + 씬 드롭다운 (D52 · `evidence/2026-08-04/palette-editor.md`) |
| **문·창 메시에 선택 표식이 없고 피킹 대상 밖이었다** | 방 껍데기 편집 | **CLOSED (2026-08-04)** | `root` 에 붙어 `contents` 피킹에 안 걸렸다 — 놓아도 다시 못 고치는 상태. `contents` 그룹 + `userData.item` 으로 |
| **팔레트를 손으로 안 눌러 봤다** — 드래그·드롭다운·이름바꾸기 | F7 사용성 | OPEN | 계산·실렌더로만 확인했다. 사람이 눌러야 아는 것들이다 (끄는 느낌·격자 체감) |
| 팔레트를 **폰에서 안 봤다** — 232px 고정폭 | 폰 사용성 | OPEN | 좁은 화면에서 3D 에 남는 폭 미확인. 접히게 할지 정한다 |
| 씬을 **여러 개 만들어 오가는 것**을 안 해 봤다 | 씬 보관함 | **CLOSED (2026-08-04)** | 프리셋 4개를 차례로 만들어 씬 5개까지 쌓이는 것 확인 (`evidence/2026-08-04/presets.md`). 복제·삭제는 여전히 코드만 |
| 빈 방에서 매번 다시 지어야 했다 | F7 편집 | **CLOSED (2026-08-04)** | `새로 ▾` 프리셋 4개 (D56 · `presets.js`) |
| **배치안 겹침을 아무도 안 봤다** — 렌더를 눈으로 보고 통과시켰다 | 무대 신뢰도 | **CLOSED (2026-08-04)** | 3D 상자 게이트 추가. 넣자마자 진짜 결함 5건(컨베이어가 격벽 관통 등) · `workstation` 원점 122mm 치우침 포함 |
| **로봇 팔을 배치 화면에 안 그린다** | F7 · 시연 인상 | **CLOSED (2026-08-04)** | 붙였다. 모듈에 하나만 두고 부모만 갈아 끼운다 · 작업 자세 `setJointsDeg` · 128,808 삼각형 실렌더 확인 |
| **`API-CONTRACT.md` 460줄 — 초과선 450 (게이트 RED)** | 마감 · 문서 무게 | OPEN | FR5 트랙(조종권 토큰·D55)이 **다른 세션에서 미커밋**으로 키웠다. 처방은 절 이관인데 **옮기는 것은 사람이 확인한다**(D18). 그쪽 세션이 닫을 때 같이 처리한다 |
| **폰에는 우클릭 메뉴가 없다** | 폰 사용성 | OPEN | 길게 누르기가 `contextmenu` 로 오는지 미확인(iOS 는 안 올 수 있다). 지금은 `90° 회전`·`삭제` 버튼이 유일한 길 |
| **가려진 물건은 눌러도 앞의 것이 잡힌다** | F7 편집 | **CLOSED (2026-08-04)** | 화면좌표 근접 피킹(20px) + **작은 것만 도와준다**(최대 변 1m 미만). 큰 것끼리 훔치던 부작용까지 수리 |
| **물건을 방 밖으로 끌 수 있다** — 클램프가 없다 | F7 편집 | **CLOSED (2026-08-04)** | 발자국 반쪽만큼 안쪽에서 멈춘다. 단일·다중 한 길 · 좌표를 정수 mm 로 |
| **폰에는 Shift·우클릭이 없다** — 다중 선택·메뉴에 길이 없다 | 폰 사용성 | OPEN | 길게 누르기·두 손가락을 쓸지 정한다 |
| FR5 6MB 를 **폰 회선에서** 받는 시간 미측정 | 폰 사용성 | OPEN | 배치 탭에 들어가면 URDF·STL 을 받는다. 느리면 켜고 끄는 스위치가 필요하다 |
| `localStorage` 용량 한계 미측정 — 씬이 늘면 언젠가 저장 실패 | 씬 보관함 | OPEN | 화면에 말하긴 한다. 팀 공유(D46)로 가면 자연히 풀린다 |
| 무대(배경) 미확정 | 시연 시나리오 작성 | **CLOSED (2026-07-30)** | 과학실험실 · 팔+AMR 배치별 생산성으로 확정 |
| PRD 목표 ↔ 방산 디지털트윈 표현이 서로 안 맞는다 | PRD·무대·완료 판정 | **CLOSED (2026-08-03)** | **①무대 교체 + 목표 유지 + 동작 축(F10) 신설** 로 닫았다 (D51). PRD·MILESTONES·SHARED-CORE·USER-REQUIREMENTS·CONSOLE-REACT·PROJECT-STATUS·SESSION-START 전파 완료 |
| ~~한 사이클의 정의 (처리량의 분모)~~ — PRD 의 유일한 진짜 블로커였다 | F8·F10 비교 전체 | **CLOSED (2026-08-03)** | **1발 해체 = 1사이클** (D51). 무대가 정해지니 분모가 따라 정해졌다 |
| 무대 소품이 아직 과학실험실이다 | S1 · 시연 화면 전부 | **CLOSED (2026-08-03)** | 소품 5개(`conveyor`·`warhead`·`chuck`·`partTray`·`blastWall`) 추가 + `examples.js` 를 해체 라인으로 교체. 규약 5개 전부 통과 · 실렌더 결함 6건 수리 (`evidence/2026-08-03/props-defense-line.md`) |
| **스테이션이 `posMm[2]` 를 버리고 바닥에 그려졌다** — 도달 판정은 3D 로 재는데 | 배치 판정 신뢰 | **CLOSED (2026-08-03)** | `layout-view.js:220` 이 `baseMm ?? 0` 이었다. `baseMm` 은 어디서도 설정 안 된다 — **링은 z=900 기준인데 물건은 바닥에** 있었다. `baseMm ?? posMm[2] ?? 0` 로 |
| `validateLayout` 이 출하 예제에서 실패하고 있었다 — 게이트가 이 함수를 안 불렀다 | 배치안 무결성 | **CLOSED (2026-08-03)** | `scripts/check/layout.sh` 신설 — 스키마·없는 부품 이름·도달 0건·부품 규약(Group·바닥 원점)을 검사하고 `all.sh` 가 자동으로 집어 간다. 부품 이름을 일부러 깨뜨려 FAIL 확인 |
| **창 유리·창틀이 `Z()` 를 건너뛰어 방 밖에 떠 있었다** (D43 거울 사상과 같은 종류) | 배치 3D 전체 · 첫 화면 | **CLOSED (2026-08-03)** | 씬 z 가 `+1.6/+3.6/+1.4` 였다. `scene-axes.sh` 의 **시험용 배치안에 창·문이 없어서** 이미 있던 `b.max.z<0.3` 검사가 헛돌았다 — 픽스처에 창 2·문 1 추가, 버그 재주입으로 FAIL 확인 |
| Dashboard 실화면 미확인 | S1 검증 | **CLOSED (2026-08-03)** | `dev:dash` 실렌더 — 소품 표시·상태줄 스테이션 이름·도달 링 정상 (`evidence/2026-08-03/props-defense-line.md` §5) |
| 컷어웨이가 격벽(prop)을 안 숨긴다 — `updateCutaway` 는 벽만 본다 | 배치 3D 보기 | OPEN | 높이 1200 으로 낮춰 우회했을 뿐 근본은 남았다. 키 큰 prop 을 컷어웨이 대상에 넣을지 정한다 |
| img2threejs 산출물↔우리 JS 규약 | S1 에셋 저작 | **CLOSED (2026-08-03)** | 컨베이어 1개로 확인 — 규약 6개 전부 통과, `Shared/assets/` 불변. **단 파이프라인은 통째로 안 썼다**(20단계·텍스처 정합 중심). 가져온 것은 *참조 옆에 렌더를 놓고 고치는 루프*이고 그게 결함 3개를 잡았다 (`evidence/2026-08-03/conveyor-prop.md`) |
| 소품이 배치 화면 안에서 안 보였다 — 단독 하네스로만 렌더했다 | S1 검증 | OPEN | `LayoutView` 에 컨베이어를 얹어 축척·조명을 본다. 소품 하나에 메시 33개라 **여러 대 깔았을 때 드로우콜**도 같이 잰다 |
| 해체 4단계 중 **어디까지를 로봇이 하나** 미정 | 사이클 안의 공정 수 · F10 분모 | BLOCKED | 팀 결정. 데모는 "신관 분리" 한 단계(= 뚜껑 풀기)로 먼저 선다 |
| 표시(미사일)와 실물(페트병)이 다르다 — AR 겹쳐 보기의 "화면=실물" 전제가 깨진다 | AR 안전 표시 | OPEN | 미사일 메시는 실물 점유 부피 이하로, 안전 범위·충돌 표시는 **실물 치수로** 그린다 (`GOAL-imitation-demo.md` §데모 과제) |
| `FR5/bridge/` 코드 없음 | F1~F6 전부 | **CLOSED (2026-07-31)** | P0~P2 완성 + 실기 첫 조그 (`evidence/2026-07-31/fr5-first-motion.md`) |
| `web/` 코드 없음 | F1~F4 | **CLOSED (2026-07-30)** | `AR/` · `Dashboard/` 로 갈라 Vite 이관 완료 |
| URDF·STL 복사 | F2 | CLOSED (2026-07-29) | `scripts/assets/sync-from-unity.sh`로 자동화됨 |
| 그리퍼(PGEA-100-40)가 URDF에 없음 | F2 부분 | OPEN | URDF에 그리퍼 링크 추가. 통합형/분리형 STL 중 선택 |
| Fairino 파이썬 SDK가 PyPI에 없어 설치 경로 미확인 | V0 | **CLOSED (2026-07-31)** | 순수 표준 라이브러리 확인 → `fairino_sdk/` 벤더링 (D42) |
| macOS에서 FAIRINO C# SDK 직접 연결 가능 여부 | H0·V0 | **CLOSED (2026-07-31)** | 실물에서 version·6축·TCP readback 성공. Python SDK는 별도 갭 (`evidence/2026-07-31/fr5-live-readback.md`) |
| 같은 FR5라도 다른 개체 배정 시 IP·펌웨어를 고정값으로 오인 | V0~V7 | **CLOSED (2026-07-31)** | profile+preflight 구현, 실측 모델 `FR5-V1-002(V6.0)` 등재 |
| 어댑터 xmlrpc 호출 무타임아웃 — stop/disarm 행이면 잠금 보유 채 브리지 전체가 굳는다 | V0~V2 | **CLOSED (2026-08-03)** | `_guard()` 스레드 상한 3초 + **stop 은 잠금 0.2초만 기다리고 무락 발사** (SAFETY-RULES 제3원칙) · disconnect 도 잠금 안으로. D45 |
| 실기 게이트 — 2클라이언트 409 · 10분 폴링 · 재부팅 자동기동 | V0~V2 | **CLOSED (2026-08-03)** | 셋 다 통과 (`evidence/2026-08-03/fr5-field-gates.md`). 단 프레임 공백 5.4초 1회 관측 |
| 그리퍼 SDK 시그니처·실물 정체 미확인 | P3 | **CLOSED (2026-08-03)** | 실물 PGE A-100-40 · `SetGripperConfig(4,0)` · 개폐 2회 실기 확인 (`STACK.md` §그리퍼) |
| 조종권이 이름 문자열만으로 통과한다 (hello 위장) | V2~ · P3 그리퍼 | **CLOSED (2026-08-04)** | claim 이 1회용 토큰 발급, hello·arm·disarm·release·disconnect 가 대조 (D55). 로그인 없이 닫았다 |
| 그리퍼 지령 pct ↔ 읽기 pct 방향 반대 · `GetGripperMotionDone` 필드 순서 의심 | P3 | OPEN | 사다리 1 착수 때 3점 이상 대응표 실측 → 브리지 한 곳에서 변환 (하드 룰 5) |
| **컨트롤러 충돌 감지가 켜져 있지 않다** — 게이트 조건 4·5 의 전제 붕괴 | 안전 전체 | **부분 CLOSED (2026-08-04)** | 브리지가 ARM 마다 설정을 넣고 되읽기 대조 (조건 26 · `evidence/2026-08-04/fr5-safety-settings.md`). **단 실제로 반응하는지는 미검증** — 아래 항목 |
| 충돌 민감도 실측값 미정 — SDK 등급(1~10)과 매뉴얼 N/m 은 축이 다르다 | 안전 전체 | OPEN | 보수적 값에서 한 단계씩 낮추며 실기 실측. STOP 손에 두고 |
| **충돌 감지가 실제로 반응하는지 미검증** — 등급 5 를 넣었을 뿐이다 | 안전 전체 | OPEN | 사람이 팔을 밀어 서는지 확인. 이번 변경의 진짜 완료 판정 |
| 말단 하중·설치 방향 미설정 — 드래그·충돌감지 오작동 조건 | 안전 · 모방학습 | **CLOSED (2026-08-04)** | 하중 0.6kg·무게중심 실기 적용 후 **되읽기 일치 확인**. 무게중심은 근사값(자동 인식은 나중) |
| **툴 0 에 이미 Z 135mm 오프셋이 들어 있다** — 누가 어떤 근거로 넣었는지 모른다 | TCP 표시 정확도 | OPEN | 실측(2026-08-04). 계약의 "TCP 는 플랜지 기준" 서술이 이 개체에선 틀렸다. 그리퍼 실측과 대조해 맞으면 등재, 아니면 재캘리브레이션 |
| Power Detection(출력 상한) 미설정 — SDK `SetPowerLimit` 되읽기 없음 | 접촉 충격 | OPEN | 값 근거를 못 찾았다. 충돌 등급 실측 후 함께 판단 |
| **FR-HMI(3위치 인에이블) 부재** — 컨트롤러 간섭영역·안전정지가 Enable 실패 가능 | 위치 안전망 | OPEN | 컨트롤러 위치 안전망을 방어선으로 세지 않는다. URDF 소프트리밋이 유일 (`SAFETY-RULES` §FR-HMI) |
| 부팅 후 로봇 자동 재연결 없음 + 그 사실을 팀이 알 방법 없음 | 운영 | OPEN | 서비스 기동 후 지연 `/connect` 1회 자동 시도(실패해도 무해) 또는 화면에 상시 표시 |
| 브리지 비정상 종료 시 컨트롤러 세션 점유 — 복구 힌트 없음 | V0 | OPEN | `/connect` 실패 사유에 "컨트롤러 세션 점유 의심 — 재부팅" 힌트 추가 |
| 브리지 로그가 `journalctl`(ssh 보유자)에게만 보인다 | 운영 | OPEN | 최근 N줄 링버퍼 + `/debug/log` 읽기 전용 (LAN 팀신뢰 전제) |
| 모방학습 — 뎁스카메라 스트림의 소유 브리지·화면 미정 | 신규 요구 | **CLOSED (2026-08-03)** | 주인은 라즈베리파이 4 (전원도 파이가 댄다). 계약 `API-CONTRACT.md` §카메라 · D48 |
| 서보 스트리밍 경로 없음 — `MoveJ` 로는 정책의 초당 수십 프레임을 못 받는다 | 모방학습 전체 | OPEN | `ServoMoveStart`/`ServoJ`(8ms)/`ServoMoveEnd` 로 브리지에 스트리밍 모드 신설 (D47 · STACK 등재됨, 실기 미검증) |
| 스트리밍용 안전 게이트 미구현 — "명령당 5°" 는 점 대 점 전제다 | 모방학습 안전 | OPEN | 각속도 ≤30°/s · 경과시간 비례 · 프레임 결손 3회면 종료 (`SAFETY-RULES.md` §서보 스트리밍 상한) |
| 텔레오퍼레이션 입력 장치 없음 — ±1° 버튼으로 30~50회 시연은 불가 | 시연 수집 | OPEN | **먼저 직접교시로 우회 가능한지 본다**(아래 행) — 되면 이 갭은 안 사도 닫힌다. 안 되면 게임패드·3D 마우스를 서보 스트리밍 경로에 연결 |
| **직접교시가 실기에서 되는지 미검증** — `DragTeachSwitch` 는 SDK 에 있다(`Robot.py:943`) | 시연 수집 (1순위 후보) | OPEN | 킬실험 30분 — ①걸었을 때 팔이 사람 힘으로 밀리나 ②그 동안 20004 관절값이 따라오나. **선행: `SAFETY-RULES` 조건 25**(드래그 중 stop 외 차단) |
| 드래그 티칭 중 원격 명령 차단이 화면 힌트뿐 — 서버 게이트가 없다 | 직접교시 안전 | OPEN | `SAFETY-RULES` 조건 25 구현. 사람이 팔을 잡고 있는데 원격 조그·arm 이 들어오는 경로를 서버가 막는다 |
| 그리퍼 `force` 상한 미설정 — SDK 기본값에 기대고 있다 | 뚜껑 잡기·체결 안전 | OPEN | 실물로 잡는 힘 하한(뚜껑을 놓치지 않는)과 상한(변형 안 되는)을 재고 `STACK.md` 에 등재 |
| 학습용 GPU·손목 뎁스카메라 미확보 | 정책 학습 | BLOCKED | 카메라 기종(D405 유력)·예산·GPU 확보는 팀 결정. 파이 4 전원 여유(5V·3A)와 저전압 경고를 첫날 확인할 것 (D48) |
| `/state` 간헐 JSON 깨짐 (char 521 부근, 전이 순간) | V0·V1 | **CLOSED (2026-08-03)** | 브리지 무죄 — 개발 맥의 rtk 훅이 파이프 출력 510B에서 잘라 `[full output: …]` 꼬리를 붙인 것. 파일 저장·`rtk proxy curl` 은 100% 정상 (95회 실측) |
| 실기 P2 잔여 게이트 — 2클라이언트 409·10분 폴링 | V2 | OPEN | 다음 현장 세션에서 각 5분 |
| `fairino_cs/`(폐기된 C# 경로) 보존 여부 | — | OPEN | 증거 보존 중 — 다음 정리 때 archive 이관 판단 |
| 씬 축 매핑이 거울이었다 (`planToScene` det −1) | 3D 전체·AR 오버레이 | **CLOSED (2026-08-02)** | D43 · 17곳 수정 + `check/scene-axes.sh` 게이트 (`evidence/2026-08-02/cam-overlay.md`) |
| 글로벌 카메라 캘리브레이션 도구 없음 | AR 오버레이 | **CLOSED (2026-08-02)** | `scripts/map/` 5개 + 게이트 3개. 합성 검증 0.83px |
| 카메라 스트림을 어느 브리지가 소유하나 | G4 라이브 영상 | **CLOSED (2026-08-03)** | 손목 카메라는 파이(D48). **고정 글로벌 카메라는 별개** — 그건 아직 미정이다 |
| 렌즈 왜곡 언디스토트 경로 없음 | AR 오버레이 정합 | OPEN | three.js 는 핀홀만 그린다. k1 −0.1 이면 모서리 97px. 실측 k1 을 재고 영상 언디스토트를 넣는다 |
| **글로벌 카메라 하드웨어 미확보** | AR 오버레이 전부 | OPEN | 있는 USB 웹캠은 **640×480 고정**(3코덱 실측) → 실제 맵에서 1.9 px/칸, 안전선의 1/3. 태그로 못 메운다. 1080p 이상 필요 — 갤럭시 임시 가능. **잠글 것 5개: 초점(AF)·노출(AE)·HEIF·손떨림보정·삼각대 강체 고정.** 폰 앱 설정은 우리 코드가 못 잠근다(사람이 한다). 촬영 중 상시충전은 스웰링 위험 (`evidence/2026-08-03/global-cam-aim.md`) |
| 카메라 자리를 계산으로 못 정한다 (화각↔캘리브 순환) | 카메라 설치 | **CLOSED (2026-08-03)** | `map/aim.py` — 실시간 px/칸 판정. 정확한 투영 대비 2% 이내 |
| 높이 정합이 사진으로 검증 안 됨 | AR 오버레이 | OPEN | 합성 사진에 높이 있는 실물이 없다. 실제 맵에서 벽을 세우면 닫힌다 |
| 컷어웨이가 벽만 숨기고 문짝·문틀은 남긴다 | 배치안 3D 보기 | OPEN | 숨긴 벽의 문 유리가 허공에 뜬다. `updateCutaway` 가 문 부품도 같이 토글해야 한다 |
| `mapToLab` 측정 절차 미정 | AMR↔FR5↔AR 연결 전부 | OPEN | TB-CONTRACT §미래접점③ 이 "P4 에서 정한다"로만 남아 있다. **이번 트랙 최대 관문** |
| `docs/evidence/2026-07-31/` 15건 > 상한 14 | 게이트 레드 | **CLOSED (2026-08-03)** | tb 검증 mock+실기 두 파일을 `tb-mock-verify.md` 한 건으로 합쳐 14 로 |
| tb-bridge 실기 배포 (우분투 real 어댑터) | 터틀봇 실기 관제 | **CLOSED (2026-08-03)** | `ej@192.168.11.2` 기동·실렌더 6/6 (`evidence/2026-07-31/tb-mock-verify.md` §P4) |
| 로봇 실주행 (bringup·teleop·SLAM) | 터틀봇 실주행 전부 | **BLOCKED** | WiFi 교체 예정 → 교체 후 로봇 bringup(도메인 0) 켜고 재개. `config.yaml`만 갱신 |
| 실물 그리퍼·tool/user·페이로드·충돌 형상 미보정 | V3·V5·V6 | BLOCKED | 현장 값 확정 전 시뮬레이션 후보를 “최적” 또는 실기 승인으로 승격하지 않는다 |
| 폰 HTTPS 접속 방식 미결정 (mkcert vs 터널) | V3 | BLOCKED | 개발 환경과 시연 환경에서 접속 방법 확정 |
| 기록 저장소 미정 (파일/SQLite/Supabase) | F6, V4 | BLOCKED | 저장 방식과 보존 기간 팀 회의에서 확정 |
| v3 기능이 실제 동작하는지 미검증 — 이름만 확인. V3 씬은 빌드에 없음 | F1·F3 | OPEN | 옮길 기능마다 원본 C# 직접 읽기. 안전 로직 우선 (`docs/evidence/2026-07-29/v3-feature-survey-limits.md`) |
| SDK 상태 패킷이 안전조건을 주는지 | V0 전체 | **CLOSED (2026-07-29)** | 킬-실험 통과 — 4개 값 전부 확인. `docs/ref/contract/SAFETY-RULES.md` |
| 비상정지 신호의 SDK 필드명 | 안전 전체 | **CLOSED (2026-07-30)** | `EmergencyStop` 외 9개 확인. `docs/evidence/2026-07-30/sdk-state-fields.md` |
| **편집한 배치안이 저장되지 않는다** — 새로고침하면 사라진다 | L1 · L3 비교 | **CLOSED (2026-07-31)** | `localStorage` 저장 + 되돌리기 한 단계. 실렌더 9항목 확인 (`evidence/2026-07-31/dashboard-l1-editor.md`). **천장 — 브라우저 한 대 안에서만 산다.** 팀 공유는 아래 항목 |
| 저장이 **브라우저 한 대 안에만** 있다 — 팀·기기 간 공유 불가 | L3 비교 · 팀 검토 | OPEN | `Shared/data/config/` 슬롯으로 올린다 (이관 H 단계) |
| `pointerup` 이 유실되면 편집기가 멈춘다 (`pointercancel` 미청취) | L1 사용성 | OPEN | **합성 이벤트에서만 재현됐다.** 실제 입력은 `setPointerCapture` 가 막는다 — 실사용에서 나오면 그때 고친다 |
| 격자 사이 좌표를 못 넣는다 (끌기가 100mm 에 붙는다) | L1 · 실측 치수 반영 | **CLOSED (2026-07-31)** | x·y·회전 숫자 입력. 실렌더 11항목 (`evidence/2026-07-31/dashboard-l1-editor.md`) |
| `userData.item` 이 `{kind,id,type}` 만 담아 **회전을 안 실어 나른다** | 3D 노드에서 값을 읽는 모든 화면 | OPEN | 지금은 **배치안에서 직접 읽어** 우회했다. `Shared/view3d/parts.js:162` 를 고치면 근본이 닫힌다 |
| **편집기를 폰에서 안 봤다** — 패널 접힘 · `SLOP_PX=4` 가 손가락에 맞나 | L1 폰 사용성 | **CLOSED (2026-07-31)** | 주인님이 폰에서 확인 — "잘됨". 자동화로는 세 번 다 막혔던 항목이다. **기종·수치는 안 남겼다** — 문턱을 다시 건드리면 그때 재확인한다 |
| 문서 무게 경고 — INDEX 54행(45) · PROJECT-STATUS 160(120) · DECISION-LOG · 07-31 evidence 10건(8) | 진입 비용 | OPEN | 2026-08-03 에 낡은 표기 정리 + dashboard 증거 5→1 통합으로 일부 줄였다. 나머지는 soft 경고(게이트 통과) — **옮기는 것은 사람이 확인하고 한다**(D18) |
| **고르기만 해도 편집이 생긴다** — 클릭 중 손이 1px 만 움직여도 격자로 스냅해 커밋 (`runN1` y 7620→7600) | L1 · 배치안 신뢰 | **CLOSED (2026-07-31)** | 끌기 문턱 `SLOP_PX = 4` 를 뒀다. 넘기 전에는 **메시도 데이터도 안 건드린다** (`interaction.js`). 배포본에서 확인 — 클릭 시 편집 0건, 끌기는 그대로 |
| 끌기가 끝나도 **미리보기 모드(`live`)가 안 꺼져** 패널이 놓은 자리에 얼어붙었다 | L1 · 배치안 신뢰 | **CLOSED (2026-07-31)** | 되돌리기를 해도 옛 좌표를 보여줬다. 커밋할 때 `live` 를 끈다 (`LayoutView.jsx`). 배포본에서 `6300,8000` → ⌘Z → `2600,7500` 확인 |
| 메인뷰가 **되는 기능을 "다음 단계"라고 적어 뒀다** | L1 사용성 | **CLOSED (2026-07-31)** | 문구 전면 교체 + 실렌더 확인 (`evidence/2026-07-31/dashboard-l1-editor.md`). 판정 `ready-for-review` — 실제 폰은 아직 |
| 벽·문·창을 클릭해도 안 골라지는데 **화면이 이유를 안 알려준다** | L1 사용성 | OPEN | 의도된 제한이다(`userData.item` 없는 노드). 지금은 고치지 않고 기록만 — 실사용에서 오해가 나오면 그때 만든다 |
| Vercel 이 모노레포 workspaces 를 못 빌드한다 (하위 폴더 `npm install` 이 `@fr5/shared` 를 못 푼다) | 배포 자동화 | OPEN | 지금은 로컬 빌드 산출물 업로드 (D24). 루트 기준 설정은 AR 과 충돌하므로 보류 |
| `Dashboard/dist` 7.2MB 중 6.6MB 가 **아직 안 쓰는** URDF·그리퍼 STL | 배포 속도 | OPEN | `publicDir` 이 `Shared/assets` 전부를 복사한다. L2 에서 팔을 세우면 쓰인다 — 그때까지 방치 |
| Dashboard 기존 CSS가 새 의미 토큰을 아직 안 씀 (`--c-ok` 선택 탭 · 밝은 화면의 `color-scheme: dark`) | Dashboard·FR5 시각 통일 | OPEN | 다음 UI 구현 때 `--c-selected`·밝은 color-scheme으로 교체. 이번 변경은 문서·토큰까지만 |
| 터틀봇 상판 마커 크기가 검출 한계인가 | AMR 위 가상 팔 | **CLOSED (2026-07-31)** | 35mm·1.8m·100% 확인. 단 `src=1280&cv=960` 필수 (`evidence/2026-07-31/marker-live-phone.md`) |
| **고해상도에서 fps 13~14** — 시각적으로 끊긴다 | AR 체감 품질 | OPEN | 검출 캔버스를 줄여도 그대로다 → 병목이 카메라 디코딩 쪽. 프레임 건너뛰며 검출하는 방식 검토 |
| **고정 카메라 경로가 아직 없다** — 맵 전체를 한 대로 찍고 AMR 위에 팔 | 시연 본편 | OPEN | 마커가 아니라 **호모그래피 4점 + 위치값**. 맵·카메라 위치 확정 후 착수. 색 검출은 사진 1장으로 먼저 판정 |
| AMR 위치(odom·AMCL)를 받을 수 있는지 미확인 | 고정 카메라 · 하이브리드 | BLOCKED | 팀원 ROS 쪽 확인 필요. 못 받으면 영상 색 검출로 우회 |
| **AR 을 영상으로 남길 방법이 없었다** — 폰 내장 녹화는 버튼바까지 찍힌다 | 시연 · 증거 | **CLOSED (2026-07-31)** | ⚙ 에 [● 녹화]. 카메라+겹침만 합쳐 mp4. 합성 레이어를 픽셀로 확인 (`evidence/2026-07-31/ar-record.md`). 판정 `ready-for-review` |
| 녹화가 **진짜 카메라 영상과 합쳐지는지 못 봤다** — 가짜 카메라 캔버스로만 확인 | AR 녹화 신뢰 | OPEN | 자동화 탭에 웹캠이 안 붙는다. **폰에서 한 판 찍어 본다** — 그때 iOS 사파리 코덱도 같이 본다 |
| 녹화 중 **fps 비용 미측정** — 프레임마다 한 장 더 그린다 | AR 체감 품질 | OPEN | 이미 13~14fps 다. 폰에서 `[측정 30초]` 를 녹화 전후로 돌려 차이를 잰다 |
| **실기가 낸 값이 기본값이 아니었다** — 마커 `#2`(실물은 `#5`) · AR.js 기본 해상도(58%) | AR 전체 | **CLOSED (2026-07-31)** | 배포본을 폰에서 열었더니 로봇이 안 떴다. `.env` 를 `#5`·45mm 로, `ar.js` 기본을 `src=1280&cv=960` 으로 (D42). 배포본에서 확인 |
| `FR5_MARKER_MM` 하한이 **설정 40 · 슬라이더 10** 으로 서로 달랐다 | 크기 시험 | **CLOSED (2026-07-31)** | 시트의 10~35mm 를 설정으로 못 박았다. `config.mjs` 를 10 으로 맞췄다 (D42) |
| **AR 새 배포본을 폰에서 안 봤다** — 마커 #5·새 기본 해상도 | AR 시연 | OPEN | `https://fr5ar.vercel.app/ar.html`. 로봇이 뜨는지 · 녹화본에 카메라 배경이 깔리는지 · fps 를 본다 |
