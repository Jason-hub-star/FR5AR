# PROJECT-STATUS — 현재 상태

**터틀봇 상판 마커가 실기로 통과했다 — 35mm · 1.8m · 인식률 100%.**
AR.js 기본 해상도가 병목이었고, 그것만 풀어 58% → 100%.  
갱신일: 2026-08-06

## 지금 단계

**주제가 확정됐다 (2026-07-30 팀 회의)** —
**방산 탄두 조립 라인에서 로봇팔과 AMR의 배치, 그리고 로봇이 움직이는 방식에 따라
생산성이 얼마나 달라지는가.** (D51 · **공정은 조립이다 D66**)
우리는 **관제화면과 AR(시각화)**을 맡고, 수치를 내는 알고리즘과 AMR 자율주행은 팀원이 맡는다.

## 완료된 것

**~07-31 — 기반은 다 섰다.** 사전 조사 · 하네스 · SSOT 7 · URDF 웹 실렌더 · 마커 재설계(#5) ·
그리퍼 장착값 · 주제 확정(D16·D17) · 문서 무게 게이트(D18) · **Vite 이관 A~D·G**(기준값 7개
일치) · **터틀봇 상판 마커 실기**(35mm·1.8m·100% · D26·D27) · **L1 배치안 편집기 배포**
(`fr5dashboard.vercel.app` · D33~D35) · **AR 녹화+배포**(`fr5ar.vercel.app` · D42).
**근거는 전부 `docs/evidence/` 에 날짜별로 있다** — 여기 다시 쓰지 않는다.

## FR5 트랙 — P0~P2 완료 (2026-07-31 · 실기 첫 조그·그리퍼 개폐)

브리지는 우분투 systemd 로 돈다 (`http://192.168.30.240:5055` · **주소 고정됨 D63**) ·
배포 `bash scripts/deploy/fr5-ubuntu.sh`. 로봇은 `192.168.58.2` — **펜던트와 같은 대역** (D68).
상세·실기 게이트는 `evidence/2026-07-31/fr5-first-motion.md` · `evidence/2026-08-03/fr5-field-gates.md`.
**다음 순서의 정본은 골사다리 6칸**(`docs/goals/GOAL-*.md`)이다 — `FR5-IMPLEMENTATION-PLAN.md`
는 화면·안전 순서의 정본이고 **완료된 것만** 적는다 (D74).

**08-05** 실기 3건(주소 `58.2` D68 · ARM 모드 복구 D72 · 작업영역 게이트 D73) + **사다리 2
(Teach) 완료**(D74·D75). **08-06** 글로벌 카메라 PiP 실기 완료(D79) · UI/UX 감사 반영·배포.

⚠ **방어선 하나가 비었다** — `motionQueueLength` 가 이동 중에도 **0** 이라 조건 6·`EXECUTING`·드리프트 검사가 다 헛돈다. **감사 1순위.**

**08-07 캘리브레이션 닫힘 + 폰 가로 장착 (D82).** `fx 2106 · 62.6°` 가 근거 없음으로 드러나
다시 쟀다 — **`fx 1728.17 · HFOV 73.05° · RMS 0.469px`** + `labToCam`(0.95mm/px). 관건은
장수가 아니라 **보드 기울기**. 가로로 돌리며 CSS 90° 회전이 사라져 **겹치기 선행 조건이
닫혔다.** 로봇 베이스 `(285,-179,1.5)mm · yaw 177.1°`. **PiP 끊김 자동 복구 배포 (D83).**

**08-07 밤 — 카메라 상태 HUD + 게이트 자동화 (D84·D85).** 판정을 `Shared/data/camera/` 한
곳으로 올려 FR5·AR 이 같은 문장·색을 쓴다. 그 과정에서 **AR 겹침이 2176px 어긋난 것**을
찾았다(합성 사진에 실측 캘리브가 씌워져 있었다 → **0.80px**). 그걸 잡는 게이트는 손으로
부르는 `.mjs` 라 며칠간 아무도 안 봤다 — 실렌더를 전부 자동 게이트에 잠갔다.
`all.sh` **2분 51초 · 370건**.

⚠ **`AR/` 재배포 필요**(옛 `fx 2106`) · **폰 설정이 앱 재시작마다 되돌아간다**(D64 · 하루 5회,
찍기 전 `cam-lock.sh` 필수) · **`/status.json` CORS 실기 미확인**(막혀 있으면 폰이 켜져 있어도
`설정 확인 못 함` 이 상주한다).

## 미착수

- FR5 사다리 4 (History) · URDF 확장 — **사다리 3(Program 슬롯)은 08-06 에 닫혔다**
- **다음 한 걸음** — ①저장을 팀 공유로(이관 H · D46) ②L2 AR 검증 → L3 비교

## 터틀봇 관제 트랙 (2026-07-31 개설 · D29~D31)

**AMR 기종 확정 — TurtleBot3 Burger ×2** (eduwing-robotics/ros2-ai-amr-repo2 ·
ROS 2 Jazzy · Nav2). 팀원 전원이 URL 하나로 켜고·몰고·매핑하고·기록 보는
관제 대시보드를 `TurtleBot/`(웹 + tb-bridge)에 세운다. 계약 `docs/ref/contract/TB-CONTRACT.md` 완료.

주행 알고리즘은 팀원 몫 — `bridge/slots/` 슬롯 계약으로 꽂힌다.

**2026-08-06 — 실기 배포 완료.** `http://192.168.30.240:5056` (FR5 와 **같은 기계**라 포트로
가른다 · D80). systemd `--user tb-bridge` 로 돌고 배포는 `bash scripts/deploy/tb-ubuntu.sh`
(8.8초 · 실렌더 6/6 PASS · adapter:real · 로봇 카드 2). mock 은 `bash scripts/dev/tb-dev.sh`.
**남은 건 로봇 전원뿐** — 도메인 0·1·2·30 토픽 0개. 근거 `evidence/2026-08-06/tb-connect-check.md`.

## 블로커

- ~~"한 사이클의 정의" 미합의~~ → **해소 (D51·D66)** — **1발 조립 = 1사이클**
- 실험 시나리오(스테이션 종류·개수)·작업대 높이 미확정 — L1을 부분적으로 막는다
- **조립 공정 중 로봇이 맡을 범위 미정** — 팀 결정 대기 (GAP BLOCKED)
- ~~Fairino Python SDK 미확인이 V0 어댑터를 막는다~~ → **해소(D42)** — 원본 확인 결과
  순수 표준 라이브러리라 macOS 에서 그대로 돈다. 실기 첫 조그까지 통과

## 관제화면 골사다리 (2026-08-04 개설 · 4칸 · `docs/goals/GOAL-dash-*.md`)

**대시보드가 맵 편집기에 머문 원인은 빈 폴더 하나였다** — `Shared/data/datasource/`.
문서 2개가 SSOT 로 선언했는데 실체가 없어 지표를 받아올 데가 없었다.

| 칸 | 골 | 상태 |
|---|---|---|
| 1 | 화면이 데이터 출처를 모르게 | **닫음** · 실렌더 17/17 · 게이트 신설 |
| 2 | 시간축 재생기 — 작업물이 라인을 흐르고 탄두가 쪼개진다 | **닫음** · 실렌더 28/28 · 게이트 신설 |
| 3 | L3/F8 — "A가 B보다 몇 % 낫다" | 미착수 |
| 4 | L4/F10 — 동작 3벌 비교 (와우모먼트) | 미착수 |

**저작 사다리 셋도 닫았다 (2026-08-04 · 실렌더 113/113)** — 화면이 **편집기**가 됐다
(시나리오 저장·자세 저작·AMR 경로 · `evidence/2026-08-04/dash-authoring.md`). **무대도 다시
섰다 (D59·D60·D66)** — 탄두는 고정, 팔 둘이 리프터를 마주 보고 역할이 갈린다(도달 922mm 인데
팔레트↔리프터 4.6m). ⚠ **실물 FR5 는 한 대다** — 화면의 두 대는 무대 구성이다.

## 핸드오프 (2026-08-07 · 저장소 D81·파이5 확정 · 다음은 **기록 노드 구현**과 사다리 4 History)

**08-07 — 저장소를 SQLite 로 되돌렸다 (D81).** 공유층은 랩 안 상주 기계의 SQLite,
클라우드는 "랩 밖에서 봐야 하는가" 가 참이 될 때. 호스트는 **안 정했다** (계약이 호스트를
모르는 게 정상 · D61 태도). 같은 날 궤적 `stamp` 에 **그리퍼 정체**를 실었다 — 값은 오고
있었는데 로그로 버려지고 있었다. 근거 `evidence/2026-08-07/storage-decision-and-stamp.md`.
⚠ **실기 미검증** — mock 으로만 확인했다. 다음 실기에서 활성화→녹화→JSON 의 `stamp.gripperConfig` 를 연다.

**08-07 밤 — 파이 실측 완료. 호스트는 파이5 로 확정.** 실험 5개 통과 — 전원 `0x0` ·
브리지 **HTTP 200** · **내장 RTC(`/dev/rtc0`)** · SD 14.9G · 4코어 3.9GB. 전원 껐다 켜고
**랜선 뽑은** 상태에서 WiFi(`192.168.30.45`) 자동 접속 + 브리지 재접속까지 무인 복구.
내장 RTC 가 파이4(외부 모듈 필요) 대비 결정타. 근거 `evidence/2026-08-07/pi5-log-node-bringup.md`.
접속 — `pi`@`fr5-log`(WiFi `.45` · 이더넷 `.44`) · Ubuntu Server 24.04.3.

⚠ **딸린 사고** — macOS `debugfs -w` 로 SD ext4 편집하다 카드 두 장(로봇 kim · team1)
shadow 손상. ext4 쓰기는 리눅스에서. 우분투 PC + USB-A 리더로 복구 예정(kim 해시 확보).

### 다음 세션 — 기록 노드 구현 (파이5 확정 후)

- **기록 DB 매체** — SD 는 부팅용. 쓰기 잦은 DB 는 NVMe/USB3 SSD 로 (D81 조건①, 미확보)
- **SQLite 스키마 + 브리지 WS 구독** — D81 은 결정만, 구현 0줄. 스키마 전에 계약부터(하드 룰 1)
- **RTC 배터리** — `/dev/rtc0` 있으나 정전 후 시각 유지 미확인
- LLM(1~3B) 는 얹을지 미정 — RAM 3.9GB 에서 tok/s 미측정

✅ **FR5 몫은 자동 게이트에 잠겼다** — 단위 141 + 실렌더 159. 근거 `evidence/2026-08-06/fr5-gate-lock.md`.
⚠ 남은 `.mjs` 는 아직 손으로 — `dash-web`(113) · `cam-web` · `fr5-cam` · `tb-*`.
✅ `/감사` P0 는 08-05 4건 · 08-06 화면 3건 전부 닫혔다 (`evidence/2026-08-05/audit-harness-and-bridge.md` ·
`evidence/2026-08-06/fr5-uiux-audit.md`). ⚠ 남은 손 사용성 2건 — **조그 연타 · STOP 26px**(사람이 폰에서 본다).

| | FR5 브리지 | 배치 편집기 | 비전·카메라 |
|---|---|---|---|
| **시작 지점** | `docs/goals/GOAL-run-history.md` (사다리 4 · 사다리 3 은 닫혔다) | `Dashboard/src/features/layout/LayoutEditor.jsx` | `docs/research/global-camera-hud-brief.md` §7 |
| **먼저 읽을 것** | `evidence/2026-08-06/fr5-uiux-audit.md` → **D78·D75** | `evidence/2026-08-04/dash-authoring.md` → `GOAL-amr-route-legible.md` | `evidence/2026-08-07/cam-status-hud.md` → **D84** |
| **막힌 것** | **큐가 이동 중을 안 알린다(위 1)** · 막힌 경로 거부를 못 봤다 · `_guard` 상한을 넘긴 호출은 연결을 영구 오염(재연결만이 복구) · 물건 파지 미실험(힘 30% 고정) · 래치 오류·`j1` 한계 밖은 펜던트로만 | **손으로 안 눌러 봤다** — 실렌더 113/113 이지만 전부 합성 이벤트. 폰에서 팔레트 232px 고정 · Shift·우클릭이 없어 다중선택·메뉴에 길이 없다 | **폰이 꺼져 있다** — `status.json` CORS·D64 회귀를 실기에서 못 봤다. 그 뒤는 **툴 좌표계 미검증**(좌표 제안이 전부 `toolCalibrationUnverified`)이고, 그 앞에 **저울이 없다** |
| **첫 명령** | `bash scripts/check/all.sh` → 폰에서 조그 연타·STOP 크기 손으로 확인 | `bash scripts/check/all.sh` → `npm run dev:dash` | `bash scripts/map/cam-lock.sh --host <폰>` → 브리프 §7 단계 3(공간 HUD) |

**카메라 조사는 실측으로 닫혔다** (D69~D71 · `evidence/2026-08-05/cam-fov.md`) — 장착 기하·화각·사각지대·유효율·USB3.2.
**로봇 상태 (08-06 실측)** — `58.2`(D68) · **ARMED · 서보 ON · owner admin** · 오류 0/0. `~/fr5-data/` 에 지점 `home`·`1`, 궤적 `실기-정지-01`.

## AR 트랙 — 카메라가 붙었다 (2026-08-04 갱신)

소프트웨어는 사진 한 장이면 끝까지 돈다 (실렌더 정합 0.83px · 전체 사슬 1.27px).
인쇄물도 준비됐다 — A4 6장(`Shared/assets/tag/`), 실측 검은 사각형 **145mm**.

**하드웨어 블로커가 풀렸다** — 갤럭시가 2560×1440 을 준다(A4 ChArUco 코너 35/35).
⚠ **이 폰의 수동 초점은 거짓말이다**(D64) — 찍기 전 매번 `scripts/map/cam-lock.sh`.
상세 `evidence/2026-08-04/global-cam-phone.md`.

**길이 갈렸다 (D67)** — 마커 제약("iOS 가 안 열어준다")은 **남의 폰**의 것이었다.
**WebXR(ARCore)** 은 준비 0 · 20초 · 축척 오차 ±2%.
`AR/xr.html` (`fr5ar.vercel.app/xr.html`) — **모드 넷 · 바텀시트**. 평소(1:2.5) · **답사(1:1 ·
걸어 들어간다)** · 촬영(1:1+두 모서리) · 화면(XR 없이 · PC 확인용). 고를 것은 **모드와 맵 둘**뿐.

**정합을 손에서 센서로 넘겼다 (D76 · 실기 확인)** — 최대 오차는 캘리브레이션이 아니라 손으로
돌리던 `↺↻` 15° 였다(12m 에서 157cm). 히트 포즈의 법선을 **받아 놓고 버리고 있었다.** 벽이
정밀함을, **사람이 서 있는 쪽**이 방향(90° 네 자리)을 낸다. 링이 맵의 어디인지는 모드가 정한다
— 평소 가운데 · **답사 들어가는 변의 가운데**(앞으로 걸어 들어간다) · 촬영 모서리. `WARMUP_MS`
는 **준비도**로 바뀌었다. 근거 `evidence/2026-08-07/xr-calibration.md`. 마커는 정밀 정합용.

**다음 한 걸음** — ① **폰 로그를 회수한다**(`결과 복사`) — `앵커 붙음` · 그림자 켠 뒤 fps ·
**그 방에서 벽이 평면으로 잡히는가**. 걸어 들어가기는 확인됐고 이 셋은 아직 못 봤다
② **3250×3250 · 벽 250mm 맵** ③ `zones[]` 진입 판정. 남은 관문 — **`mapToLab`** · XR 원점 리셋.

## 하네스 (2026-07-30 한국어 배선 · D19)

슬래시 명령은 **`.claude/skills/` 의 한글 폴더명이 결정한다.** 전체 목록은 `/명령어`.
**개수는 여기 적지 않는다** — `bash scripts/check/skills.sh` 가 정본이고 어긋나면 게이트가 실패한다.
