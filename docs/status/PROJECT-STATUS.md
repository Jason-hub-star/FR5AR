# PROJECT-STATUS — 현재 상태

**터틀봇 상판 마커가 실기로 통과했다 — 35mm · 1.8m · 인식률 100%.**
AR.js 기본 해상도가 병목이었고, 그것만 풀어 58% → 100%.  
갱신일: 2026-07-31

## 지금 단계

**주제가 확정됐다 (2026-07-30 팀 회의)** —
**방산 탄두 해체 라인에서 로봇팔과 AMR의 배치, 그리고 로봇이 움직이는 방식에 따라
생산성이 얼마나 달라지는가.** (D51)
우리는 **관제화면과 AR(시각화)**을 맡고, 수치를 내는 알고리즘과 AMR 자율주행은 팀원이 맡는다.

SSOT·문서 분할은 D16·D17, **Vite 이관 A~D·G 는 완료**(기준값 7개 일치 ·
`evidence/2026-07-30/ar-baseline.md`).

## 완료된 것

**~07-30** — 사전 조사 3건 · 하네스 이식 · SSOT 7개 · URDF 웹 실렌더 · 마커 재설계(#5) ·
그리퍼 장착값 · 웹 화면 3 · 공유 모듈 3 · Vercel 배포(D11) · 폰 확인(D15) · 깃허브 공개(D14) ·
Vite 관문 · 주제 확정(D16·D17) · 문서 무게 게이트(D18).
**2026-07-31** — `web/` 정리 · 관제화면 별도 배포(D24) · 폰 측정 도구(`[측정 30초]`).
**근거는 전부 `docs/evidence/` 에 날짜별로 있다** — 여기 다시 쓰지 않는다.

- **터틀봇 상판 마커 실기 통과** (35mm·1.8m·100%, D26·D27) · **L1 배치안 편집기 완결·배포**
  (`fr5dashboard.vercel.app`, 실렌더가 버그 12개, D33~D35) · **AR 녹화+배포**
  (`fr5ar.vercel.app`, D42) · 디자인 규약(D38) · 증거 폴더 규약(D37) · observe-only 기준선(D39)
- 상세는 `evidence/2026-07-31/` 의 `marker-live-phone` · `dashboard-l1-editor` · `ar-record` ·
  `fr5-live-readback` 에 있다

## FR5 트랙 — P0~P2 완료 (2026-07-31 · 실기 첫 조그·그리퍼 개폐)

브리지는 우분투 systemd 로 돈다 (`http://192.168.30.240:5055` · **주소 고정됨 D63**) ·
배포 `bash scripts/deploy/fr5-ubuntu.sh`. 로봇은 `192.168.57.2` — **컨트롤러 1번 포트** 기준.
상세·실기 게이트는 `evidence/2026-07-31/fr5-first-motion.md` · `evidence/2026-08-03/fr5-field-gates.md`,
다음 순서는 `docs/ref/plan/FR5-IMPLEMENTATION-PLAN.md` 와 골사다리 6칸이 정본이다.

## 미착수

- FR5 P3~(Teach·Program·Optimize·History 패널) · 그리퍼 시그니처·손가락 개폐 · URDF 확장
- **다음 한 걸음** — ①저장을 팀 공유로(이관 H · D46) ②L2 AR 검증 → L3 비교

## 터틀봇 관제 트랙 (2026-07-31 개설 · D29~D31)

**AMR 기종 확정 — TurtleBot3 Burger ×2** (eduwing-robotics/ros2-ai-amr-repo2 ·
ROS 2 Jazzy · Nav2). 팀원 전원이 URL 하나로 켜고·몰고·매핑하고·기록 보는
관제 대시보드를 `TurtleBot/`(웹 + tb-bridge)에 세운다. 계약 `docs/ref/contract/TB-CONTRACT.md` 완료.

주행 알고리즘은 팀원 몫 — `bridge/slots/` 슬롯 계약으로 꽂힌다.

**P0~P4 완료 (로봇 실주행만 보류)** — 상세는 `evidence/2026-07-31/tb-mock-verify.md`.
mock 실행 `bash scripts/dev/tb-dev.sh`. real 브리지는 우분투(`ej@192.168.11.2`)에서 떴다.
**로봇 실주행만 남았다** — bringup(도메인 0)이 안 켜져 토픽이 없었고, **WiFi 교체 예정**이라
교체 후로 보류. 재개: 우분투 `bash ~/start-bridge.sh`(코드 `~/fr5tb/`).

## 블로커

- ~~"한 사이클의 정의" 미합의~~ → **해소 (D51)** — **1발 해체 = 1사이클**
- 실험 시나리오(스테이션 종류·개수)·작업대 높이 미확정 — L1을 부분적으로 막는다
- **해체 4단계 중 로봇이 맡을 범위 미정** — 팀 결정 대기 (GAP BLOCKED)
- ~~Fairino Python SDK 미확인이 V0 어댑터를 막는다~~ → **해소(D42)** — 원본 확인 결과
  순수 표준 라이브러리라 macOS 에서 그대로 돈다. 실기 첫 조그까지 통과

## 관제화면 골사다리 (2026-08-04 개설 · 4칸 · `docs/goals/GOAL-dash-*.md`)

**대시보드가 맵 편집기에 머문 원인은 빈 폴더 하나였다** — `Shared/data/datasource/`.
문서 2개가 SSOT 로 선언했는데 실체가 없어 지표를 받아올 데가 없었다.

| 칸 | 골 | 상태 |
|---|---|---|
| 1 | 화면이 데이터 출처를 모르게 | **닫음** · 실렌더 17/17 · 게이트 신설 |
| 2 | 시간축 재생기 — 작업물이 라인을 흐르고 탄두가 쪼개진다 | **닫음** · 실렌더 28/28 · 게이트 신설 |
| 3 | L3/F8 — "A가 B보다 몇 % 낫다" | 미착수 |
| 4 | L4/F10 — 동작 3벌 비교 (와우모먼트) | 미착수 |

**무대도 다시 섰다 (D59·D60)** — 탄두는 **고정**되고 팔이 움직인다. `arm` 하나가
`arms[]` 가 되어 **FR5 두 대**가 리프터를 마주 보고, 방은 **9:16 세로**이며 컨베이어
한 줄이 방을 관통해 밖으로 나간다. 남쪽 끝은 **램프**로 280mm 까지 내려와 AMR 이 올린다.
**팔도 이제 고르고 옮긴다.**

⚠ **실물 FR5 는 한 대다** — 화면의 두 대는 무대 구성이고 실측을 나눠 적지 않는다.

**2칸이 3칸보다 먼저인 이유** — L4 는 재생기가 전부이고 L3 는 표 하나다. 위험이 큰 쪽 먼저.
**컨베이어를 돌리는 코드가 아니라 `series` 재생기**를 짠다 — 같은 코드가 목업 · 팀원 sim ·
동작 3벌 · 기록 되감기 네 곳을 먹는다.

## 핸드오프 (2026-08-04 · 두 트랙이 나란히 갔다)

**게이트는 전부 green 이다.** 앞선 판의 "게이트 RED · 미커밋 섞임" 경고는 해소됐다 —
`API-CONTRACT.md` 는 배치안·지표를 `LAYOUT-METRICS-CONTRACT.md` 로 갈라 322줄이 됐고,
FR5 트랙(D53~D55)은 전부 커밋됐다.

| | FR5 브리지 트랙 | 배치 편집기 트랙 |
|---|---|---|
| **시작 지점** | `docs/goals/GOAL-live-gripper.md` (사다리 1/6) | `Dashboard/src/features/layout/LayoutEditor.jsx` |
| **먼저 읽을 것** | **D63** → `evidence/2026-08-04/fr5-network-recovery.md` → `fr5-{safety-settings,maintainability}.md` | **D56~D58** → `evidence/2026-08-04/editor-ux.md` |
| **막힌 것** | 코드는 **실기 반영 완료** · 읽기까지 확인. 남은 것은 **유선 고정 미완**(저장값이 DHCP 로 되밀린다)과 **쓰기 경쟁 미측정** | **손으로 안 눌러 봤다**(전부 합성 이벤트) · 폰 미검증 |
| **첫 명령** | `bash scripts/check/all.sh` → `/우분투` | `bash scripts/check/all.sh` → `npm run dev:dash` |

**FR5 브리지 (2026-08-04)** — 상세는 `evidence/2026-08-04/fr5-{safety-settings,maintainability}.md`

- **공식 매뉴얼 7종을 코드와 대조** → 게이트의 전제가 없었다. 컨트롤러 충돌 감지는 기본으로
  안 켜지고 기본 민감도는 사람 접촉에 무반응 — **조건 4·5 가 종이호랑이였다**. 브리지가
  ARM 마다 설정을 넣고 되읽어 대조하게 했다 (D53 · 조건 26 신설)
- **실기가 바로 잡아 준 것** — j3 소프트리밋이 우리(±162)가 컨트롤러(±160)보다 넓었다.
  툴 0 에 이미 Z 135mm 오프셋이 있어 계약의 "TCP 는 플랜지 기준" 서술이 틀렸다
- **유지보수 정비 4건** (D54·D55) — 세션을 클래스로(`main.py` 463→328) · `safety.py` 단위
  테스트 29 · **조종권 토큰**(이름만으로 뚫리던 구멍) · 실렌더 검증을 `data-t` 로 분리
- 검증: 브리지 **42/42** · 실렌더 21/21 · 단위 29 · 게이트 전부 통과

**배치 편집기 (2026-08-03~04)** — 상세는 `evidence/2026-08-03/props-defense-line.md` ·
`evidence/2026-08-04/{palette-editor,presets,editor-ux}.md`

- **무대를 방산 탄두 해체 라인으로** (D51). "한 사이클의 정의"가 **1발 해체 = 1사이클**로 풀렸다
- **팔레트 편집기 + 프리셋 4개** (D52·D56) — 빈 방에서 짓거나 프리셋을 꺼낸다. 썸네일은 코드로 굽는다
- **선택 표식은 발자국 윤곽** (D57) — 외접원을 버렸다. 발광은 공유 재질이라 막힌 길
- **편집이 틈·묶음·크기로 간다** (D58) — 치수 가이드 · Shift 다중 선택 · 크기 조절 · 우클릭 메뉴
- **FR5 팔을 배치 화면에 붙였다** (128,808 삼각형 · 작업 자세)
- **게이트가 잡은 것** — 배치안 겹침 5건(컨베이어가 격벽 관통) · `workstation` 원점 122mm 치우침 ·
  `chair({hMm})` 가 인자를 안 씀 · `blastWall` 캡 z-fighting(윗면을 두 번 놓쳤다)
- **게이트 2 신설·1 수리** · `docs/ref/` 카테고리화(md 17개 → 5폴더)

**다음 세션 첫 작업** — **손으로 눌러 본다.** 이번 세션 것이 전부 합성 이벤트라
손맛(끄는 느낌·격자 체감)과 폰이 통째로 미검증이다. 그 뒤 P2: 터틀봇 GLB
(저장소에 없다 — ROBOTIS 공식 URDF 에서 변환) · FR5 다중 배치는 `layout.arm` 단일 필드라
`SHARED-CORE.md` 가 먼저다 (하드 룰 1). 폰에는 Shift·우클릭이 없어 **다중 선택·메뉴에
길이 없다** — 길게 누르기를 쓸지 정한다.

**로봇 상태** — 이번 세션에 실기를 붙이지 않았다. 마지막 상태 그대로 OBSERVE_ONLY.

## AR 트랙 — 카메라가 붙었다 (2026-08-04 갱신)

소프트웨어는 사진 한 장이면 끝까지 돈다 (실렌더 정합 0.83px · 전체 사슬 1.27px).
인쇄물도 준비됐다 — A4 6장(`Shared/assets/tag/`), 실측 검은 사각형 **145mm**.

**하드웨어 블로커가 풀렸다** — 640×480 웹캠 대신 **갤럭시가 2560×1440** 을 준다
(유선·무선 처리량 동일). 그 해상도에서 A4 ChArUco 가 **코너 35/35** 라 A3 재인쇄는 없다.
⚠ **이 폰의 수동 초점은 거짓말이다**(D64) — `focusmode=auto` + `/focus` 1회로 맞추고
판정은 **선명도 수렴**으로 한다. 찍기 전 매번 `scripts/map/cam-lock.sh`.
상세 `evidence/2026-08-04/global-cam-phone.md`.

**현장 순서·명령·폰 설정은 `research/ar-global-camera.md` §B 가 정본이다.** 첫 걸음은
바닥 원점 테이프, 카메라 자리는 `scripts/map/aim.py` 가 초록을 띄우는 지점.
⚠ `tagSizeMm` 은 파일값이 아니라 **인쇄물 실측값**을 쓴다 (틀리면 높이가 10% 밀린다).

**다음 한 걸음** — `capture.py charuco` 15~20장 → `intrinsics.py`. 자동초점이라
**보드 거리 말고 각도·화면 위치를 바꿔** 찍는다. 그 다음 태그 배치 → `extrinsics.py`.

남은 관문 — `mapToLab` 측정 절차(**최대**) · 렌즈 왜곡 · 라이브 MJPEG(G4) · 스트림 주인
미정(손목 카메라는 D48 로 파이 확정). 근거는 D43 · D64 와 위 evidence 셋.

## 하네스 (2026-07-30 한국어 배선 · D19)

슬래시 명령은 **`.claude/skills/` 의 한글 폴더명이 결정한다.** 한국어 스킬 **15개**
(메타 5 · 도메인 5 · 신설 5) · 커맨드 0 · 훅 3 · 게이트 7. 전체 목록은 `/명령어`.

**개수는 상태가 아니라 상수다** — `bash scripts/check/skills.sh` 가 정본이고, 어긋나면
게이트가 실패한다. 여기 숫자를 믿지 말고 그 명령을 돌린다.
