# PROJECT-STATUS — 현재 상태

**터틀봇 상판 마커가 실기로 통과했다 — 35mm · 1.8m · 인식률 100%.**
AR.js 기본 해상도가 병목이었고, 그것만 풀어 58% → 100%.  
갱신일: 2026-07-31

## 지금 단계

**주제가 확정됐다 (2026-07-30 팀 회의)** —
**방산 탄두 조립 라인에서 로봇팔과 AMR의 배치, 그리고 로봇이 움직이는 방식에 따라
생산성이 얼마나 달라지는가.** (D51 · **공정은 조립이다 D66**)
우리는 **관제화면과 AR(시각화)**을 맡고, 수치를 내는 알고리즘과 AMR 자율주행은 팀원이 맡는다.

SSOT·문서 분할은 D16·D17, **Vite 이관 A~D·G 는 완료**(기준값 7개 일치 ·
`evidence/2026-07-30/ar-baseline.md`).

## 완료된 것

**~07-30** — 사전 조사 3건 · 하네스 이식 · SSOT 7개 · URDF 웹 실렌더 · 마커 재설계(#5) ·
그리퍼 장착값 · 웹 화면 3 · 공유 모듈 3 · Vercel 배포(D11) · 폰 확인(D15) · 깃허브 공개(D14) ·
Vite 관문 · 주제 확정(D16·D17) · 문서 무게 게이트(D18).
**2026-07-31** — `web/` 정리 · 관제화면 별도 배포(D24) · 폰 측정 도구(`[측정 30초]`).
**근거는 전부 `docs/evidence/` 에 날짜별로 있다** — 여기 다시 쓰지 않는다.

- **터틀봇 상판 마커 실기 통과** (35mm·1.8m·100%, D26·D27) · **L1 배치안 편집기 완결·배포**
  (`fr5dashboard.vercel.app`, 실렌더가 버그 12개, D33~D35) · **AR 녹화+배포**
  (`fr5ar.vercel.app`, D42) · 디자인 규약(D38) · 증거 폴더 규약(D37) · observe-only 기준선(D39)
- 상세는 `evidence/2026-07-31/` 의 `marker-live-phone` · `dashboard-l1-editor` · `ar-record` ·
  `fr5-live-readback` 에 있다

## FR5 트랙 — P0~P2 완료 (2026-07-31 · 실기 첫 조그·그리퍼 개폐)

브리지는 우분투 systemd 로 돈다 (`http://192.168.30.240:5055` · **주소 고정됨 D63**) ·
배포 `bash scripts/deploy/fr5-ubuntu.sh`. 로봇은 `192.168.58.2` — **펜던트와 같은 대역** (D68).
상세·실기 게이트는 `evidence/2026-07-31/fr5-first-motion.md` · `evidence/2026-08-03/fr5-field-gates.md`.
**다음 순서의 정본은 골사다리 6칸**(`docs/goals/GOAL-*.md`)이다 — `FR5-IMPLEMENTATION-PLAN.md`
는 화면·안전 순서의 정본이고 **완료된 것만** 적는다 (D74).

**08-05 실기 3건** — ①**주소가 `58.2` 로 옮겨갔다**(D68) ②**ARM 이 펜던트를 잠그고 있었다**
— `mode` 명령 + 헤더 토글로 되돌린다(D72) ③**작업영역 게이트**(D73) — 로봇이 상판 3점·벽
2점을 직접 짚어 등재했고, 손끝이 상판을 뚫거나 벽으로 가는 목표를 거부한다.
근거는 `evidence/2026-08-05/{workcell-measure,pendant-mode}.md`.

**08-05 밤 — 사다리 2(Teach)를 실기까지 닫았다 (D74·D75).** 화면이 **4패널**이 됐다
(Optimize 삭제 — 비교의 주인은 관제화면이다). 지점(점)·궤적(선)·이동 전 미리보기.
궤적은 **잰 조건**(`stamp`·`fps`·`startPose`·`endReason`·`dropped`·`purpose`)을 함께 실어
F10 비교가 거짓말하지 않게 한다. 브리지는 `teach.py`·`commands.py` 로 갈라
**main.py 623 → 431줄**(형제 tb-bridge 327/21 대비 425/21).

**실기** — 캡처(관절 소수 4자리 일치) · 궤적 6.196s/93프레임 **결손 0** · 배포 2회 생존 ·
**`home`→`1`→`home` 왕복 성공**. 실기가 결함 둘을 잡았다: **녹화 주기가 폴링과 같으면
결손이 필연**(→15fps) · **`MoveJ` 기본이 블로킹이라 33° 이동이 3초 상한을 넘겨 xmlrpc 연결을
오염**시켰다(→`blendT=0.0`). 근거 `evidence/2026-08-05/teach-points-trajectory.md`.

⚠ **그 과정에 방어선 하나가 비었다** — `motionQueueLength` 가 이동 중에도 **0** 이라
조건 6·`EXECUTING`·드리프트 검사가 다 헛돈다. **다음 세션 감사 1순위.**

## 미착수

- FR5 사다리 3~4 (Program 슬롯 · History) · URDF 확장
- **다음 한 걸음** — ①저장을 팀 공유로(이관 H · D46) ②L2 AR 검증 → L3 비교

## 터틀봇 관제 트랙 (2026-07-31 개설 · D29~D31)

**AMR 기종 확정 — TurtleBot3 Burger ×2** (eduwing-robotics/ros2-ai-amr-repo2 ·
ROS 2 Jazzy · Nav2). 팀원 전원이 URL 하나로 켜고·몰고·매핑하고·기록 보는
관제 대시보드를 `TurtleBot/`(웹 + tb-bridge)에 세운다. 계약 `docs/ref/contract/TB-CONTRACT.md` 완료.

주행 알고리즘은 팀원 몫 — `bridge/slots/` 슬롯 계약으로 꽂힌다.

**P0~P4 완료 (로봇 실주행만 보류)** — 상세는 `evidence/2026-07-31/tb-mock-verify.md`.
mock 실행 `bash scripts/dev/tb-dev.sh`. real 브리지는 우분투(`ej@192.168.11.2`)에서 떴다.
**로봇 실주행만 남았다** — bringup(도메인 0)이 안 켜져 토픽이 없었고, **WiFi 교체 예정**이라
교체 후로 보류. 재개: 우분투 `bash ~/start-bridge.sh`(코드 `~/fr5tb/`).

## 블로커

- ~~"한 사이클의 정의" 미합의~~ → **해소 (D51·D66)** — **1발 조립 = 1사이클**
- 실험 시나리오(스테이션 종류·개수)·작업대 높이 미확정 — L1을 부분적으로 막는다
- **조립 공정 중 로봇이 맡을 범위 미정** — 팀 결정 대기 (GAP BLOCKED)
- ~~Fairino Python SDK 미확인이 V0 어댑터를 막는다~~ → **해소(D42)** — 원본 확인 결과
  순수 표준 라이브러리라 macOS 에서 그대로 돈다. 실기 첫 조그까지 통과

## 관제화면 골사다리 (2026-08-04 개설 · 4칸 · `docs/goals/GOAL-dash-*.md`)

**대시보드가 맵 편집기에 머문 원인은 빈 폴더 하나였다** — `Shared/data/datasource/`.
문서 2개가 SSOT 로 선언했는데 실체가 없어 지표를 받아올 데가 없었다.

| 칸 | 골 | 상태 |
|---|---|---|
| 1 | 화면이 데이터 출처를 모르게 | **닫음** · 실렌더 17/17 · 게이트 신설 |
| 2 | 시간축 재생기 — 작업물이 라인을 흐르고 탄두가 쪼개진다 | **닫음** · 실렌더 28/28 · 게이트 신설 |
| 3 | L3/F8 — "A가 B보다 몇 % 낫다" | 미착수 |
| 4 | L4/F10 — 동작 3벌 비교 (와우모먼트) | 미착수 |

**저작 사다리 셋도 닫았다 (2026-08-04 · 실렌더 113/113)** — 화면이 **편집기**가 됐다.
시나리오 저장 · 자세 저작(관절 링 + URDF 한계) · AMR 경로(그리고·따라가고·읽는다).
시점도 고쳤다 — `frame()` 이 세로 화각만 봐 가로 화면에서 방이 **13.6%** 였다(→ 59.8%).
상세 `evidence/2026-08-04/dash-authoring.md` · 골 `docs/goals/GOAL-amr-route-legible.md`.

**무대도 다시 섰다 (D59·D60·D66)** — 탄두는 **고정**되고 팔 둘이 리프터를 마주 본다.
투입 팔과 조립 팔로 역할이 갈리고(도달 922mm 인데 팔레트↔리프터가 4.6m), 방은 9:16 세로다.
⚠ **실물 FR5 는 한 대다** — 화면의 두 대는 무대 구성이고 실측을 나눠 적지 않는다.

## 핸드오프 (2026-08-05 밤 · `/감사` 완료 · 다음은 **P0 수리**)

⚠ **"게이트 전부 green" 을 믿지 않는다.** `all.sh:11` 은 `*.sh` 만 돌아 `.mjs` 7개를
아무도 안 부른다 — **단위 106 만 자동이고 브리지 82 · 실렌더 46 은 손으로 불러야 나온다.**

**`/감사` 29건 (P0 4 · P1 16 · P2 9).** 전문 `evidence/2026-08-05/audit-harness-and-bridge.md` ·
추적 GAP-MATRIX 상단 9행. **코드는 안 고쳤다(주인님 판정: 보고서만).**
P0 — ①위 게이트 ②궤적 이름이 그대로 파일 경로(임의 덮어쓰기) ③`NaN` 이 안전설정 대조를
통과해 **하중 없이 ARM** ④지점 삭제에 확인·되돌리기 없음. **①부터** — 게이트가 반만 돌면
나머지 수리의 회귀를 못 잡는다. 표적 ① 근원은 `read_state()` 가 SDK 의 `robot_state`·
`motion_done`·`actual_qd` 를 안 읽는 것. 확정하려면 **로봇을 움직여야** 한다.

| | FR5 브리지 | 배치 편집기 | 비전·카메라 |
|---|---|---|---|
| **시작 지점** | `docs/goals/GOAL-program-slots.md` (사다리 3) | `Dashboard/src/features/layout/LayoutEditor.jsx` | `docs/ref/arch/DEPTH-CAM.md` |
| **먼저 읽을 것** | **D75·D74** → `evidence/2026-08-05/teach-points-trajectory.md` | `evidence/2026-08-04/dash-authoring.md` → `GOAL-amr-route-legible.md` | **D70·D71** → `evidence/2026-08-05/cam-fov.md` |
| **막힌 것** | **큐가 이동 중을 안 알린다(위 1)** · 막힌 경로 거부를 못 봤다 · `_guard` 상한을 넘긴 호출은 연결을 영구 오염(재연결만이 복구) · 물건 파지 미실험(힘 30% 고정) · 래치 오류·`j1` 한계 밖은 펜던트로만 | **손으로 안 눌러 봤다** — 실렌더 113/113 이지만 전부 합성 이벤트. 폰에서 팔레트 232px 고정 · Shift·우클릭이 없어 다중선택·메뉴에 길이 없다 | **툴 좌표계 미검증** — 좌표 기반 제안이 전부 `toolCalibrationUnverified` 로 거부. **저울이 없어 말단 하중을 못 넣어** 드래그 티칭이 막혀 있다 |
| **첫 명령** | `all.sh` 가 `.mjs` 를 집게 고치기(P0-1) → 나머지 P0 | `bash scripts/check/all.sh` → `npm run dev:dash` | `bash scripts/check/all.sh` → 툴 좌표계 캘리브레이션 |

**카메라 조사는 실측으로 닫혔다** (D69·D70 · `evidence/2026-08-05/cam-fov.md`) —
장착 기하·화각·사각지대·유효율 임계값·USB3.2 까지. 문서 무게 게이트도 개정됐다 (D71).

**로봇 상태** — `58.2`(D68) · OBSERVE_ONLY · 서보 ON · 오류 0/0. 지점 `home`·`1` 과
궤적 `실기-정지-01` 이 `~/fr5-data/` 에 살아 있다.

## AR 트랙 — 카메라가 붙었다 (2026-08-04 갱신)

소프트웨어는 사진 한 장이면 끝까지 돈다 (실렌더 정합 0.83px · 전체 사슬 1.27px).
인쇄물도 준비됐다 — A4 6장(`Shared/assets/tag/`), 실측 검은 사각형 **145mm**.

**하드웨어 블로커가 풀렸다** — 갤럭시가 2560×1440 을 준다(A4 ChArUco 코너 35/35).
⚠ **이 폰의 수동 초점은 거짓말이다**(D64) — 찍기 전 매번 `scripts/map/cam-lock.sh`.
상세 `evidence/2026-08-04/global-cam-phone.md`.

**길이 갈렸다 (D67)** — 마커 제약("iOS 가 안 열어준다")은 **남의 폰**의 것이었다.
**WebXR(ARCore)** 은 준비 0 · 20초 · 축척 오차 ±2%.
`AR/xr.html` (`fr5ar.vercel.app/xr.html`) — **모드 넷 · 바텀시트**. 평소(1:2.5) · **답사(1:1 ·
걸어 들어간다)** · 촬영(1:1+두 모서리) · 화면(XR 없이 · PC 확인용). 고를 것은 **모드와 맵 둘**뿐.

**정합을 손에서 센서로 넘겼다 (D76 · 실기 확인)** — 최대 오차는 캘리브레이션이 아니라 손으로
돌리던 `↺↻` 15° 였다(12m 에서 157cm). 히트 포즈의 법선을 **받아 놓고 버리고 있었다.** 벽이
정밀함을, **사람이 서 있는 쪽**이 방향(90° 네 자리)을 낸다. 링이 맵의 어디인지는 모드가 정한다
— 평소 가운데 · **답사 들어가는 변의 가운데**(앞으로 걸어 들어간다) · 촬영 모서리. `WARMUP_MS`
는 **준비도**로 바뀌었다. 근거 `evidence/2026-08-07/xr-calibration.md`. 마커는 정밀 정합용.

**다음 한 걸음** — ① **폰 로그를 회수한다**(`결과 복사`) — `앵커 붙음` · 그림자 켠 뒤 fps ·
**그 방에서 벽이 평면으로 잡히는가**. 걸어 들어가기는 확인됐고 이 셋은 아직 못 봤다
② **3250×3250 · 벽 250mm 맵** ③ `zones[]` 진입 판정. 남은 관문 — **`mapToLab`** · XR 원점 리셋.

## 하네스 (2026-07-30 한국어 배선 · D19)

슬래시 명령은 **`.claude/skills/` 의 한글 폴더명이 결정한다.** 한국어 스킬 **16개**
(메타 5 · 도메인 6 · 신설 5) · 커맨드 0 · 훅 3 · 게이트 7. 전체 목록은 `/명령어`.

**개수는 상태가 아니라 상수다** — `bash scripts/check/skills.sh` 가 정본이고, 어긋나면
게이트가 실패한다. 여기 숫자를 믿지 말고 그 명령을 돌린다.
