# PROJECT-STATUS — 현재 상태

**터틀봇 상판 마커가 실기로 통과했다 — 35mm · 1.8m · 인식률 100%.**
AR.js 기본 해상도가 병목이었고, 그것만 풀어 58% → 100%.  
갱신일: 2026-07-31

## 지금 단계

**주제가 확정됐다 (2026-07-30 팀 회의)** —
**과학실험실에서 로봇팔과 AMR의 배치에 따라 생산성이 얼마나 달라지는가.**
우리는 **관제화면과 AR(시각화)**을 맡고, 수치를 내는 알고리즘과 AMR 자율주행은 팀원이 맡는다.

그에 맞춰 **SSOT를 다시 썼다.** 목표·기능(F7~F9)·요구정의서(P6·UR_21~25·SR_23~26)·
마일스톤 순서(L1~L3이 V0 앞으로)를 갱신하고, 문서를 **번들 기준으로 갈랐다** —
`SESSION-START.md` §폴더 라우터가 입구다. 근거는 `DECISION-LOG` D16·D17.

**Vite 이관 A~D 완료.** 엔트리 4개가 다 빌드되고 **기준값 7개가 이관 전후로 일치했다**
(`evidence/2026-07-30-ar-baseline.md`). **남은 미확인은 폰의 카메라 하나다.**
남은 단계는 수렴 루프로 정렬했다 (`ref/rnd/MIGRATION-CONVERGE-LOOP-2026-07-30.md`, 0.588→0.858).

**L1 이 섰다** — 관제화면에서 실험실을 3D 로 보고 물건을 끌어 옮긴다.
아직 **저장이 없다**. 그게 다음 한 걸음이다.

## 완료된 것

**~07-30** — 사전 조사 3건 · 하네스 이식 · SSOT 문서 7개 · URDF 웹 실렌더 ·
마커 재설계(바코드 #5) · 그리퍼 장착값 · 웹 화면 3개 · 공유 모듈 3개 ·
Vercel 배포(D11) · 폰 실기 확인(D15) · 깃허브 공개(D14) · Vite 관문 ·
주제 확정과 SSOT 재작성(D16·D17) · 문서 무게 게이트(D18).
**근거는 전부 `docs/evidence/` 에 날짜별로 있다** — 여기 다시 쓰지 않는다.

- **`web/` 정리 완료** (2026-07-31) — 12파일·2.2MB 삭제. 이관표 전수 대조 후.
  Vercel 링크는 `AR/.vercel` 로 이전 (`docs/ref/BUILD-VITE.md` §파일이 어디로 갔나)
- **터틀봇 상판 마커 실기 통과** (2026-07-31) — **35mm · 1.8m · 인식률 100%.**
  AR.js 가 폰에서 카메라 640×480 · 검출 320×240 으로 두 번 깎고 있었다.
  둘 다 올려 58%→100%. 합성 이미지가 낸 24px 기준은 **2배 비관적**이었다
  (D26·D27 · `evidence/2026-07-31-marker-live-phone.md`)
- **폰에서 한 판을 끝내는 측정 도구** (2026-07-31) — `⚙` 의 [측정 30초] ·
  dev 서버 로그 싱크(`.diag/`) · `scripts/dev/report-diag.py`. 노트북을 오가지 않는다
- **L1 배치안 편집기 — 고르고 · 끌고 · 돌린다** (2026-07-31).
  흰색 모형 실험실 12×8m (방·문·창·가구 21개, 텍스처 0). 클릭 피킹 + 100mm 격자 스냅 +
  놓을 때 커밋. **폰 터치도 된다.** 실렌더로 버그 4개를 잡았다 —
  카메라 탈취 · 커밋마다 시점 리셋 · 링 좌표계 혼용 · 죽은 `pickRoot`
  (`evidence/2026-07-31-dashboard-editor.md`)
- **관제화면 별도 배포** (2026-07-31) — `fr5dashboard` 프로젝트. AR 과 분리한 이유는 D24.
  재배포는 `bash scripts/dev/deploy-dashboard.sh` (**부를 때만** 돈다)

## 미착수

- `server/` 브리지 서버 코드 없음
- 그리퍼 손가락 개폐 (prismatic 관절) — 데모 범위 밖. 벌어진 채로 고정
- URDF 확장 (그리퍼를 코드가 아니라 URDF에 넣기) — 장착값은 이미 실측해 뒀다
- (완료 2026-07-29) URDF+STL과 그리퍼 메시를 `Shared/assets/`로 복사 — 팔 58,482 + 그리퍼 70,102 = 128,584 삼각형

## 다음 한 걸음

`BUILD-VITE.md` §이관 순서가 정본이다. **A~D·G 완료** — 엔트리 4개가 빌드되고
기준값 7개가 일치한다. 남은 것은 E(AR 새 프로젝트 배포)·F(폰 확인).

1. **L1 마무리 — 편집 결과를 저장한다.** 지금은 React 상태에만 있어 **새로고침하면 사라진다.**
   `Shared/data/config/` 슬롯으로 내린다 (= 이관 H 단계와 같은 일이다)
2. **E — 새 Vercel 프로젝트 `fr5ar`(소문자)로 `AR/` 배포.**
   기존 `web` 은 건드리지 않는다 → **지시 받고 한다**
3. **F — 폰 확인. 카메라 하나만 본다** (주인님)
4. ~~**G — 통과 후 정리.** `web/` · 죽은 파일 9개(2.2MB)~~ **완료 2026-07-31** —
   `web/` 삭제(12파일·2.2MB), Vercel 링크는 `AR/.vercel` 로 이전
5. L2 AR 검증 → L3 비교 (목업)

**AR 쪽은 한 단계 앞서 나갔다** — 터틀봇 상판 마커가 실기로 통과했다(35mm·1.8m·100%).
남은 값은 **fps 13~14**(고해상도에서 끊겨 보임)와 **고정 카메라 경로**다.

## 블로커

- **"한 사이클의 정의" 미합의** — 처리량의 분모다. 어긋나면 배치안 비교 자체가 무의미해진다.
  **팀원과 합의해야 하는 유일한 블로커**이고, L3의 판정에 걸린다
- 실험 시나리오(스테이션 종류·개수)·AMR 기종·작업대 높이 미확정 — L1을 부분적으로 막는다
- Fairino 파이썬 SDK 설치 경로 미확인 (PyPI 없음) — V0를 막는다. L1~L3에는 영향 없다

## 핸드오프 (2026-07-31 · 마커 실측 세션)

| | |
|---|---|
| **시작 지점** | `docs/evidence/2026-07-31-marker-live-phone.md` — 이번에 잰 것과 **안 잰 것**이 다 있다 |
| **먼저 읽을 것** | 그 evidence → `DECISION-LOG` **D26·D27·D28** (해상도 · 합성이미지 금지 · 고정카메라) |
| **첫 명령** | `bash scripts/check/all.sh` |
| **막힌 것** | 고정 카메라 경로가 **맵·카메라 위치 확정을 기다린다.** 그 전엔 색 검출을 튜닝할 수 없다 |

**다음 세션이 고를 갈래는 둘이다.**

1. **L1 저장** (원래 다음 걸음) — 편집기는 서지만 새로고침하면 사라진다.
   `Dashboard/src/features/layout/LayoutEditor.jsx` 의 `edits` 를 `Shared/data/config/` 슬롯으로.
2. **고정 카메라 1단계** — 호모그래피 4점 보정 화면. **환경과 무관해서 지금 지어도 안 버려진다.**
   색 검출·ROS 어느 쪽으로 가든 그 위에 얹힌다.

**안전한 다음 행동** — 맵이 아직 없으므로 1번이 기본이다. 2번을 먼저 하고 싶으면
보정 화면까지만 짓고 검출은 손대지 않는다.

**측정을 다시 할 때** — 절차는 `evidence/2026-07-31-marker-live-phone.md` §어떻게 쟀나.

## 하네스 (2026-07-30 한국어 배선)

슬래시 명령은 **`.claude/skills/` 의 한글 폴더명이 결정한다** (`~/.claude/commands.md`).
영문 스킬 12개 + 커맨드 8개를 **한국어 14개로 합쳤다** (D19). 흡수된 원본은
각 스킬의 `references/` 로 이관했다 — 삭제 0.

| 구분 | 명령 |
|---|---|
| 메타 5 (전역 고정) | `/상태` `/다음` `/진단` `/마감` `/명령어` |
| 도메인 4 | `/스택가드` `/정합` `/검진` `/슬라이스` |
| 신설 5 (영문 흡수) | `/감사` `/계획` `/검증` `/설계` `/회고` |

| 항목 | 개수 |
|---|---|
| 스킬 (= 슬래시 명령) | **14** |
| 커맨드 (`.claude/commands/`) | **0** — 전부 흡수. 의도한 값이다 |
| 훅 | 3 (self-review-gate · doc-drift-reminder · session-gate) |
| 게이트 (`scripts/check/`) | 7 (all · assets · consts · docs · docs-weight · harness · skills) |

검증 — `bash scripts/check/skills.sh` (스모크테스트: 한글 호출 가능 · frontmatter ·
references 실재 · 고아 · **없앤 이름을 부르는 곳**)
