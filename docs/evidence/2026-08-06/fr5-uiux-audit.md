# FR5 화면 UI/UX 감사 — 초심자 기준으로 다시 읽었다 (2026-08-06)

`/감사` · 대상 `FR5/` 8개 소스 1,646줄 전부 직접 읽음 + 계획·계약 SSOT 대조.

## 판정

**AI 슬롭은 0건이다.** 실측 — 이모지 0 · 과장 어휘(`원활`·`강력한`·`혁신`·`효율적으로` 외 12종) 0.
문구는 전부 구체 사건에 묶여 있었다. 문제는 반대쪽이었다:

1. **실패를 "없음" 이라고 말하는 곳이 세 군데** 있었다 — 화면이 거짓말을 했다
2. **못 누르는 이유가 툴팁에만** 있었다 — 손가락에는 안 뜬다
3. **개발자 내부어가 그대로** 나갔다 — `OBSERVE_ONLY` · `observe-only` · `tool0/user0` · `demo·measure·done`

## 고친 것

### P0 — 화면이 거짓말하거나 되돌릴 수 없는 것

| 무엇 | 어디 | 왜 P0 인가 |
|---|---|---|
| `run()` 에 `try/finally` 가 없었다 | `LivePanel.jsx:34` | 브리지가 못 답하면 `setBusy(false)` 가 영영 안 돌아 **연결·조종권·ARM·DISARM 이 전부 죽은 채로** 남았다. 새로고침 말고 나갈 길이 없었다. Teach·Program 은 이미 이 모양이었고 **Live 만 남아 있었다** |
| 읽기 실패를 삼켜 "없습니다" 를 띄웠다 | `TeachPanel.jsx:113`<br>`ProgramPanel.jsx:30` | 목록을 못 읽어도 `[]` 로 남아 *"아직 지점이 없습니다"* · *"아직 프로그램이 없습니다"* 가 떴다. **서버에는 멀쩡히 있다** — 학생은 처음부터 다시 만든다. SAFETY-RULES 제1원칙의 화면판 |
| 프로그램 삭제에 확인이 없었다 | `ProgramPanel.jsx:199` | 지점 삭제는 2단계 확인을 붙였는데(2026-08-06 오전) **프로그램만 한 번에 사라졌다.** 계획 §확인 절차는 "삭제가 모두 이 형태다" 다 |

없음과 못 읽음은 이제 다른 문장을 쓴다 — `points-loaderr` · `trajs-loaderr` · `slots-loaderr`.
세 패널의 `run()` 은 못 닿았을 때 **"브리지에 닿지 못했습니다"** 를 말한다.

### P1 — 학생이 못 넘는 문턱과 죽은 공간

| 무엇 | 어디 |
|---|---|
| **폰에서 카메라 PiP 가 데스크톱 크기 그대로 떴다** — 인라인 `style` 이 미디어쿼리를 항상 이긴다. 저장된 340px 이 3D 를 절반 넘게 가렸고, 주석은 그걸 막으려던 것이었다 | `CamView.jsx:96` ↔ `main.css:275` |
| **첫 화면 죽은 공간** — 연결 전 관절·TCP 12행이 전부 `—`. 340px 사이드의 세로 대부분을 뜻 없는 대시가 먹었고, 정작 "지금 무엇을 하면 되나" 는 없었다 → 표는 연결됐을 때만, 그 자리에 `startguide` 5칸 | `LivePanel.jsx` |
| **툴팁 전용 안내** — `title` 은 터치에 안 뜬다. Program 은 이미 `blockedWhy()` 로 문장을 줬는데 **그 규칙이 한 패널에 갇혀 있었다** → `capture-blocked`·`goto-blocked`·`rec-blocked`·`claim-blocked`·`arm-blocked`·`write-blocked` | 4개 패널 |
| **이름이 첫 관문인데 안내가 툴팁 하나뿐** → 칸이 스스로 말한다 (`data-need`) | `main.jsx` |
| **말투 두 벌** — 존댓말은 Program 7곳뿐, 나머지는 반말체 → 전부 존댓말 | 4개 패널 |
| **내부어** → `OBSERVE_ONLY 보기만 함` · `연결 (보기만 · observe-only)` · `공구0 · 기준0` · `사람 시연(demo) · 비교용(measure)` · `브리지 재접속` | 전역 |
| **`1단계로 돌아가기` 가 로봇을 움직인다고 읽혔다** — 실제로는 화면이 세는 칸만 되돌린다 → `세는 자리만 1단계로` + 설명 한 줄 | `ProgramPanel.jsx:189` |

### P2

`기록 —` → `기록 안 함`(칸은 계획이 정한 8개 그대로) · History 툴팁 `골사다리 2~4` → `4` ·
`AGENTS.md` 가 아직 "지금은 `live/` 만 산다" 라고 했다(실제 넷) · 죽은 CSS 규칙 1개 ·
접힘·폰에서 리사이즈 핸들 제거(끌어도 안 먹으면서 값만 저장됐다)

## 게이트를 하나 **강화**했다

`fr5-web-verify.mjs:341` 은 *"끊기면 관절값이 `—`"* 를 봤다. 표를 아예 안 그리면 그 의도
(**옛 값이 남으면 안 된다**)를 더 세게 만족하지만 단언은 깨진다 — 그래서 약화가 아니라
**강화로 고쳤다**: 표 자체가 `null` 인지 + 시작 순서가 「로봇에 연결」을 짚는지.

삭제 2단계 확인도 검사로 박았다 — 없으면 다음 세션에 확인 단계가 조용히 사라진다.

## 검증

```
브리지 왕복 — 93/93 PASS
웹 실렌더  — 66/66 PASS   (63 → 66 · 시작 순서 1 + 삭제 확인 2)
bash scripts/check/all.sh → exit 0
npm run build:fr5 → 872.72 kB / gzip 235.28 kB
```

브리지 왕복은 **첫 실행에서 한 번 빨갰다가 재실행에서 초록이 됐다.** 브리지 코드는 이번에
손대지 않았고, 증상(앞 검사 하나가 밀리면 뒤가 줄줄이 빨개진다)이 `GAP-MATRIX` 12행에
이미 등재된 고정 `sleep` 흔들림과 같다. **이번 변경 탓이 아니지만 게이트 신뢰도 문제로 남아 있다.**

## 안 고치고 남긴 것

| 무엇 | 왜 |
|---|---|
| **폰 보기 전용** (계획 §폰) | `GAP-MATRIX` 11행에 이미 OPEN. 조작 컨트롤을 렌더에서 빼는 별도 작업이고 화면 문구 수리와 범위가 다르다 |
| **작업영역 경계** (Live 필수) · **경로 고스트** (Teach 필수) | `GAP-MATRIX` 84·83행 OPEN. 3D·브리지 쪽 일이다 |
| **tool/user 좌표계 지정** (Teach 필수) | **이번에 새로 등재했다.** 좌표계는 지금 브리지 `config.yaml` 이 정하고 화면에 길이 없다 — `toolCoordId` 를 브리지가 로봇에 안 넣는 미해결(`GAP-MATRIX` 92행)과 같은 뿌리라 그게 먼저다 |
| **조그 연타 · STOP 26px** | `GAP-MATRIX` 18행의 남은 2건. 손 크기·연타는 실렌더가 못 만든다 — 사람이 폰에서 본다 |
| History 패널 | 사다리 4. 계획대로 미착수 |
| Program `grip`/`wait` · 버전 diff | `PROGRAM-CONTRACT` 가 천장으로 명시 — 갭이 아니다 |
