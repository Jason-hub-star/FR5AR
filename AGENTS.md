# AGENTS — FR5Web

Codex·OpenCode 등 다른 에이전트의 진입 문서. Claude와 **같은 SSOT를 공유**한다.

## 규칙 3개

1. `docs/SESSION-START.md`를 먼저 읽는다. 공통 라우팅은 전부 거기에 있다.
2. **작업 도메인 문서만 로드한다.** 백로그·조사 리포트·evidence를 사전 로드하지 않는다.
3. 작업 트리를 보존하고, 끝나면 `CLAUDE.md`의 검증 명령을 실행한다.

## 정찰 위임 규약

넓은 사전 조사는 OpenCode에 맡기되, 결과는 **한 파일로만** 회수한다.

- 산출 파일명을 정찰마다 다르게 한다 (`scratchpad/scout-<이름>.md`)
- `docs/status/SCOUT.md`는 배턴 파일이므로 병렬 정찰이 건드리지 않는다
- **리포트는 증거가 아니다.** 개수·경로·버전은 회수한 쪽이 다시 센다

## 편집 위임 시 반드시 박을 것

- "N개 전부. 하나라도 빼면 실패다. 끝나고 직접 세어 N/N을 리포트에 적어라."
- 파괴적 이관은 3단으로: 복사 → 확인 → 그제서야 원본 삭제
- "파일을 통째로 재작성하지 마라, 부분 편집만"
- 수정 허용 파일을 화이트리스트로 열거

## "없다"고 말하기 전에 (실패 사례에서 나온 규칙)

2026-07-29에 두 번 틀렸다. 둘 다 **좁은 검색어로 부분만 보고 "없음"이라고 단정**한 것이다.

| 사례 | 잘못 | 실제 |
|---|---|---|
| v3의 고스트·궤적·작업영역·충돌 표시 | 파일 6개만 받아보고 "없음" | 안 받은 루트 파일에 4개 다 있었음 |
| SDK의 드리프트 값 | `drift`·`deviation`으로만 검색 | `cmdPointError`라는 이름으로 있었음 |

**규칙 4개**

1. **부분 표본이면 "없음"이라 쓰지 않는다.** "받은 N개 파일엔 없음"이라고 범위를 밝힌다.
2. **이름으로 찾기 전에 목록을 통째로 뽑는다.** 구조체 필드·함수·파일 목록을 먼저 나열하고 훑는다.
   특히 FAIRINO SDK는 **중국어 주석**이라 영어 관용어로 찾으면 놓친다.
3. **동의어를 최소 3개 시도한다.** drift / deviation / error / offset / diff 처럼.
4. **개수를 보고할 때는 세는 단위를 함께 적는다.** 2026-07-30에 필드 수를 150→292→150으로
   두 번 바꿔 말했다. 둘 다 맞았다 — 150은 구조체 필드, 292는 별칭 포함 추출 건수였다.
   단위를 안 밝히면 정정이 또 다른 혼선을 만든다.
5. **"없음"이 결론을 바꾸는 자리면 전수로 확인한다.** 계획을 기각하거나 공수를 재산정하는 근거라면
   표본으로 끝내지 않는다.

## 하드 룰

`CLAUDE.md`의 하드 룰 6개가 그대로 적용된다. 특히 **로봇에 명령을 보내는 코드**와 **API 계약 변경**은 문서를 먼저 고친다.
